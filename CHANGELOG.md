# I SAW — 本次工作記錄

> 專案：`~/Library/Mobile Documents/com~apple~CloudDocs/Vibe Code/Photo/`
> 主檔：`film_gallery.html` ｜ 本機預覽：`http://localhost:8765/film_gallery.html`
> 啟動：雙擊 `serve.command`

---

## 1 · 專案搬遷與環境

| 項目 | 內容 |
|---|---|
| 搬遷 | `Lily_Mind/品牌專案/Photo` → **`Vibe Code/Photo`** |
| `serve.command` | 雙擊啟動本機伺服器，自動關閉佔用 8765 的舊程序、自動開瀏覽器 |

**伺服器修了三個問題**（都是我一開始埋的坑）：

1. **無快取標頭** → 你會載到舊版 HTML。加上 `Cache-Control: no-store`
2. **單執行緒** → 9 MB 的 HTML 傳輸中其他請求全部卡死。改為 `ThreadingTCPServer`
3. **不支援 HTTP Range** → 影片無法拖曳進度／跳轉。實作 `206 Partial Content`

---

## 2 · 頁首

- 標題 `Film Gallery` → **`I SAW`**，移到紅色外框區（原本在米白面板內）
- 文字標題換成布標圖 `logo@2x.png`
- 後方小字 `Look what I saw today.`
- 右側新增 **Our Story** 按鈕（正體襯線，與左側斜體對比）
- 高度以 `--hdh` 變數串連，紙張／面板／舞台的 `100vh` 計算自動跟隨

**圖檔壓縮**：`logo.png` 787 KB → `logo@2x.png` **34.7 KB**（減色版最大誤差 49/255，捨棄）

---

## 3 · Our Story 描圖紙

點擊展開三張定格動畫：**紙團 → 攤到一半 → 完全攤平**（手寫字浮現），關閉時反向收回。

| 項目 | 規格 |
|---|---|
| 圖檔 | `tracing-paper-1/2/3.png`（原檔名有空格，已統一） |
| 尺寸比例 | 70.7% / 70.7% / 100%，依原圖高度 724:724:1024 |
| 時序 | 換格 250ms、位移 400ms、關閉 190ms |
| 背景 | 只有中心徑向漸層壓暗，四角不變 |
| 模糊 | `mask-image` 取第三張 alpha 當遮罩 → **霧化精準貼合撕邊形狀** |
| 透明度 | 攤平那張 80% |

動畫從「三張各帶不同角度」改為**容器承擔單一段連續緩動**，消除逐張跳動。

---

## 4 · 蕾絲裝飾

- 頁面上方垂掛，只露下緣
- 收起 **18°** ／ 點擊滑到畫面正中央 **0°**
- 尺寸 480×480，陰影同步加深
- 三個響應式斷點（1400 / 860）
- `蕾絲.png` 3.3 MB → `lace@2x.webp` **756 KB**

---

## 5 · 配色與紙質

| 項目 | 值 |
|---|---|
| 主色 | **`#AC242E`** |
| 紅底漸層 | `#ac242e → #96202a → #7a1a22 → #5c131a` |
| 外框 | 移除 `border`，改六層自然陰影（模糊逐層加倍、透明度遞減） |

**紙質**取自你提供的 `augustine-wong-...jpg`：

```
中央裁 2000×2000 → 高通濾波（去大範圍明暗）→ 縮到 360px
→ 邊緣交叉淡化 40px（無縫平鋪）→ 灰階 → WebP
3.9 MB → tex/paper.webp 41.6 KB
```

用 `soft-light` 疊加（不是 `multiply`）— 紋理以中灰 128 為中心，soft-light 只改明暗不動色相，底色完全不被染。顯示 220px。紅底另有 `--red-tex` 可單獨調濃淡。

---

## 6 · 底片遮罩：12 個 SVG

`masks/mask-<格數>cell-<比例>.svg`，3 種版型 × 4 種比例。

**沿用你的設計邏輯**（從 `遮罩1.svg`/`遮罩2.svg` 座標反推並驗算）：

```
暖色內緣  #5E0C03，寬度 = 窗格短邊 × 12.5%，位置 = 窗格外擴 sw/2
圓角      單格 5.6%、多格 17.5%
黑色遮罩  整面減去窗格（evenodd），模糊 7
```

| 比例 | 單格 | 雙格 | 三格 |
|---|---|---|---|
| 4:5 | 你的原檔 | 你的原檔 | 620×372 |
| 9:16 | 948×1185（4:5） | 926×556 | 926×556 |
| 16:9 | 1185×948（5:4） | 879×527 | 562×337 |
| 1:1 | 948×569 | 765×459 | 470×282 |

多格一律 **5:3**。橫式 16:9 改為左右並排。

**修掉的 bug**：描邊寬度原本只依格子短邊計算，9:16 雙格算出 sw=110 但間距只有 72，上下描邊重疊後黑色遮罩蓋不住 → **咖啡色外漏**。改為 `sw = min(短邊×12.5%, 間距)`。

選單改成選「版型」，檔案依當前比例自動對應；slot 座標與 SVG 從同一份幾何產出。
`film_gallery.html` 因移除內嵌 base64 PNG，**9.3 MB → 8.0 MB**。

---

## 7 · 影片匯出：改用 Canvas

移除 `getDisplayMedia`（螢幕錄影），改為 **canvas 重繪 + MediaRecorder**。

- 不跳權限視窗、只錄中間照片區塊、不碰螢幕其他內容
- 長度 = 每張素材依類型取停留秒數**加總**
- 輸出 MP4 (H.264)，解析度跟隨「比例」設定

**通用渲染器**：遞迴走訪 `#frame` 整棵 DOM，解析每個元素的 computed style（線性／放射／重複漸層、背景圖、inset 陰影、圓角、transform、filter、blend、文字、偽元素），依 z-index 排序繪製。改 CSS 或加圖層時匯出會自動跟上。

**修掉的 bug**：
1. 暗角過重 — CSS `radial-gradient(ellipse)` 預設 `farthest-corner`，半徑需 ×√2
2. 錄到靜止畫面 — 素材影片受自動播放限制未啟動
3. 漏掉 `.masklayer` 底片框（z-index 45）
4. `#trans`(z60) 畫在 `#fade`(z58) 之前，疊圖順序顛倒

---

## 8 · 新增兩個效果 + 交叉淡入轉場

兩支參考影片以瀏覽器逐格讀取實際像素分析（無 ffmpeg／opencv）。

### 花田柔光（參考影片 02:51，745 格）

```
量測  L: p5=5 p50=87 p95=199 p99=219
      低於 16 佔 10.85%（暗部壓死）、高於 240 僅 0.07%（亮部滾降）
      飽和 .351、鄰接差 5.58、亮部 RGB 212/198/178
```

五層：媒體調色 → 暖調 multiply → 光暈 backdrop-filter screen → 壓暗 multiply → 雙層顆粒。
實作後亮部實測 **211/193/174**，與目標 212/198/178 幾乎吻合。

### 陽光褪色（參考影片 02:56，48 格）

```
量測  L: p1=41 p5=59 p50=147 p95=207 p99=218
      低於 16 僅 0.05%（暗部抬起、不壓黑）
      中位 RGB 149/152/87（黃綠）、暗部 44/65/25、亮部 215/210/170
      飽和 .376、鄰接差 9.03
```

四層：暗部 lighten 地板（直接用量測的 44/65/25）→ 黃綠 multiply → 霧化 screen → 顆粒。

### 交叉淡入（dissolve）

新畫面疊在舊畫面之上淡入，全程重疊、無黑格或遮片。實測透明度 `0 → 0.11 → 0.26 → 0.45 → 0.65 → 0.81 → 1`，重疊期間舊畫面確實存在。淡入長度 0.3–2.5 秒可調。

**修掉的 bug**：`.entering` 未清除、`build()` 清掉 `--dis` 變數、計時從呼叫時起算導致淡到 13% 就被砍斷。

---

## 兩個方法上的修正（記錄下來避免重蹈）

1. **不能用絕對直方圖比對不同素材** — 用深綠池塘影片去逼近明亮花田的直方圖，優化器被迫走向極端值（誤差從 1.17 惡化到 16.6）。應比對與素材無關的**轉換特性**（亮部色彩比例、滾降點）。

2. **參數搜尋前必須凍結畫面** — gallery 會自動輪播，前後測的不是同一張圖，分數不可比較。

---

## 待你決定

- 4:5 的單格／雙格仍是你手繪的原檔（0.739 / 1.570），要不要也統一成 5:3
- 單格的 9:16 與 16:9 我做成 4:5 與 5:4；若要字面上的 1:5 / 5:1（細長條），改 `CELL_ASPECT` 一個數字
- 遮罩產生器 `genmask.py` 目前在暫存區，要不要收進專案資料夾

---

## 9 · 匯出卡頓修正

**症狀**：存下來的影片一頓一頓。

**診斷**（用細間隔取樣算不重複影格）：
```
宣告 30fps，實際只有 6.8 fps
每一格停留 8–9 個取樣點，還夾雜 14–15 的長停留 → 不規律才是頓挫主因
```

**瓶頸**（逐層關閉量測，單位 ms/格，總計 67.4）：

| 項目 | 成本 |
|---|---|
| 底片框遮罩 | **16.1** |
| 顆粒 | 8.0 |
| 其餘（媒體繪製、混合模式填充） | 約 43 |

`getComputedStyle` 每格呼叫 106 次不是主因 — 解析度減半可快 3 倍，屬於 fill-rate bound。

**修正**：

1. **SVG 遮罩光柵化快取** — 遮罩改成 SVG 後，canvas 每格都重新光柵化（含內部高斯模糊）。改成光柵化一次存點陣圖重複使用 → **66.8 ms → 38.7 ms（15 → 25.8 fps）**
2. **改用 `captureStream(0)` + `track.requestFrame()`** — 一次渲染推一格，不再重複寫入同一張
3. **暖機量測後鎖定可達成的幀率**（留 30% 餘裕），以固定間隔推格，節奏穩定

**結果**：6.8 fps → **17.9 fps**，停留分佈從 `{8,9,14,15}` 收斂到 `{2,3,4}`。輸出幀率會顯示在狀態列。

> 註：18fps 剛好是 Super 8 的原生幀率，與這個專案的膠片調性相符。想要更高幀率可降低輸出解析度，或關掉遮罩與顆粒。

---

## 兩個濾鏡的名字

| | 名稱 | 來源 |
|---|---|---|
| 第一個 | **花田柔光**（`bloom`） | 參考影片 02:51 |
| 第二個 | **陽光褪色**（`sun`） | 參考影片 02:56 |

---

## 10 · 檔案結構整理

預設影片換成你指定的兩支，並從 **base64 內嵌改為直接引用檔案**：

| | 之前 | 現在 |
|---|---|---|
| `film_gallery.html` | 8.0 MB | **0.09 MB**（省 99%） |
| 預設影片 | 7.94 MB base64 內嵌 | `media/default-1.mov` / `default-2.mov` |
| 長寬比 | 寫死 9:16 | 從影片 metadata 自動讀取 |

用相對路徑，所以 `http://localhost:8765` 或直接雙擊開檔（`file://`）都能讀到。

### 資料夾結構

```
Photo/
├── film_gallery.html      主程式
├── serve.command          啟動本機伺服器（雙擊）
├── CHANGELOG.md
│
├── assets/                程式執行時載入
│   ├── logo@2x.png            I SAW 布標
│   ├── lace@2x.webp           蕾絲
│   ├── paper.webp             紙質紋理（無縫平鋪）
│   └── tracing-paper-1~3.png  Our Story 三格動畫
│
├── masks/                 底片框：12 個 SVG + slots.json
├── media/                 預設影片 default-1.mov / default-2.mov
├── tools/                 genmask.py（遮罩產生器）
│
├── source/                原始高解析檔，不參與執行
│   ├── logo.png、蕾絲.png、剪刀.png
│   ├── augustine-wong-...jpg  紙材原圖
│   ├── tracing paper.jpg
│   └── 遮罩1/2.png、遮罩1/2.svg（你手繪的原檔）
│
├── my-media/              你自己的照片與影片
└── experiments/           film_light_leak.html、super8_film.html
```

**驗證**：8 個靜態引用全部指向存在的檔案；12 種遮罩組合實際切換測試全部載入成功；無 console 錯誤。

---

## 11 · 介面系統化

把散落的樣式收斂成一套可重複使用的元件。

| 元件 | 規則 |
|---|---|
| **按鈕階層** | `primary` 黑底白字、hover 轉品牌紅｜`secondary` 透明底細框｜`text` 無框靠左 |
| **開關（toggle）** | 38×18 方形滑塊，開啟時滑塊變品牌紅 |
| **色票** | 一格一格直接點選，自訂色記在最後一格 |
| **參數列** | 標籤／滑桿／數字同一排（`display:contents` + flex `order`） |
| **摺疊區塊** | 大標前的 `+` / `−`，狀態記在 localStorage |

**踩到的坑**：滑桿加長後數字被擠出面板 —— flex 項目預設 `min-width:auto` 不會縮，要明確設 `min-width:0`。

---

## 12 · 轉場從濾鏡搬到「放映」

原本每個濾鏡的轉場寫死在濾鏡裡、不能調。但「放映」區問的是同一件事：畫面**之間**怎麼接，而它已經有〈入場〉〈結尾〉了——`入場 → 換場 → 換場 → … → 結尾`，只有中段被鎖住，這才是不一致的地方。

新增〈換場〉下拉選單，六個選項，選了就立刻預覽：

| 選項 | 行為 |
|---|---|
| 跟著濾鏡（預設） | 套用該濾鏡原本的轉場 |
| 交叉淡入 | 新舊畫面重疊淡入 |
| 漏光燒片 | 斜角遮片 + 8 格橘紅燒片 |
| 閃白一格 | 斜角遮片 + 12～20 格紫／青綠／粉閃格 |
| 跳黑一格 | 斜角遮片 + 黑一格 |
| 直接硬切 | 什麼都不加 |

第一個選項是「跟著濾鏡」，所以不動它就完全維持原本行為；一旦選了具體的，換濾鏡也不會被蓋掉。濾鏡說明裡的「換場景時…」那句同步移除。

---

## 13 · 輸出與預覽脫鉤

原本「輪播一輪後結尾」同時管畫板和輸出，造成兩邊都不好用。拆開：

- **畫板**：永遠一直輪播，不會停在結尾（方便邊看邊調）
- **輸出**：一輪跑完自動接上結尾

開關改名為 **「輸出時加上結尾」**，預設開啟，只影響下載。

**順手修掉兩個 bug**：

1. 錄影長度只算素材時間，結尾會被切掉 → 長度改為 `素材一輪 + 結尾`（漏光停格 0.95 秒／淡出到黑依設定）
2. 輸出時跑到的結尾會留在畫板上（畫面卡在橘褐色停格）→ 錄完自動復原

---

## 14 · 濾鏡調整

| 濾鏡 | 調整 |
|---|---|
| 時間光漏 | 光漏改為**脈衝式**（閃一下就退，不是慢慢掃過去）；新增〈光漏色〉色票 6 色 |
| Super 8 | 移除全域的〈漏光閃光間隔〉，改在 Super 8 內部加〈光漏色〉；暖棕漏光閃光整個移除 |
| 陽光褪色 | 顆粒 0.46 → 0.14；**〈暗部抬升〉修好** |
| 花田時光 | 顆粒 0.2 → 0.14 |

### 〈暗部抬升〉為什麼沒反應

不是錯覺。原本這層用 `lighten` 設一個「地板色」rgb(44,65,25)，但同一個預設的媒體濾鏡 `contrast(.78) brightness(1.28)` 已經先把最暗處抬到 **68** —— 比地板還亮。用 canvas 照圖層鏈跑數據：

| 原始亮度 | 0 | 16 | 32 | 64 | 128 | 224 |
|---|---|---|---|---|---|---|
| 滑桿 0→1 的變化量 | 24 | 11 | **0** | **0** | **0** | **0** |

只有「原圖暗於 12%」的像素會動 —— 照片裡幾乎沒有那麼暗的區域，所以整條滑桿像壞掉。

改成 `screen`（底片「黑色抬起」的正確做法，暗部抬最多、亮部完全不動）：

| 原始亮度 | 0 | 32 | 64 | 128 | 192 | 255 |
|---|---|---|---|---|---|---|
| 滑桿 0→1 的變化量 | **+45** | **+39** | **+31** | **+19** | +5 | 0 |

預設值 0.85 → 0.25，因為舊的 0.85 實際效果約等於 0，直接沿用會讓畫面突然霧掉。

---

## 15 · 新濾鏡「方寸之間」

從相機觀景窗裡看出去。這是唯一一個會**鎖住比例（4:5 1080×1350）與底片框（不用遮罩）** 的濾鏡 —— 觀景窗本身就是框。切到別的濾鏡會把原本的選擇原封不動還回去。

### 參考影片的量測

| 項目 | 結果 |
|---|---|
| 機身顏色 | = **同一高度景色的色相**，亮度 ×0.80、飽和 ×0.65（三個時間點、三個高度帶都吻合） |
| 窗內調性 | p1 = 75–84、p50 = 170–173、p99 = 225–229；**0% 低於 16、0.04% 高於 240** |
| 手持 | **慢速漂移不是抖動** —— 每 0.1 秒中位位移 0，6 秒總幅度約 4% 畫面寬 |
| 結構 | 九個各自平坦的「面」，面與面之間只有 1～2% 的窄過渡帶；四角有 25～30° 的倒角 |

因為機身顏色完全跟著景色跑，所以機身**不畫顏色、只畫明暗**，顏色由底下（後來改為上層）那張大幅模糊的照片提供 —— 換一張海邊的照片就偏藍，換草地的就偏黃綠，全自動。

### 兩次架構轉折

**第一版**用 CSS 漸層堆出機身 → 失敗。漸層只能做一維的帶狀明暗，但取景器是個雕塑（曲面、角落交會的倒角、一小片一小片的高光），所以怎麼調都像相框。

**第二版** Lily 提出「其實這次比較像請你生一張取景器的圖」——對。改用她手繪的 PNG（`assets/finder.webp`，1210×1594，有 alpha）。程式反而從 12 層簡化到 4 層：

```
win（清楚的照片）→ finder（取景器圖，透明的洞讓照片透出來）→ refl（環境反射）
```

環境反射改用 **`color` 混合**：只換色相與飽和度，保留圖上原本畫的明暗與金屬高光。

### 三個踩過的坑

1. **輸出渲染器不支援 `mask-image` / `backdrop-filter`** —— 原本想用 `backdrop-filter` 讓機身自動吃到背後模糊的景色，畫面上很漂亮但輸出會整個垮掉。全部改用 filter / mix-blend-mode / 圓角裁切達成。
2. **直式影片只看得到 17% 的高度** —— 素材原本「先鋪滿整張畫布再由窗切一塊」，主體幾乎全被裁掉。改成素材直接 cover 取景窗。
3. **窗的邊緣有明顯界線** —— 照片的硬邊落在圖檔半透明的羽化帶裡。量測：從洞緣算起 alpha 要 43～49px 才爬到 250，但原本只外擴了一半。改成外擴量跟著放大倍率走。

---

## 16 · 自製下拉選單

原生 `<select>` 在 macOS 會彈出系統的深色選單，跟介面語彙衝突。

**做法**：原生 `<select>` 留在 DOM 裡不動 —— 它照舊持有 `value` / `disabled` / `onchange`，所以程式其他地方（濾鏡鎖定、遮罩比例對應、輸出尺寸判斷…）**一行都不用改**，風險最低。只是視覺上藏起來，另外畫一顆按鈕與一張清單。

全站 6 個選單（比例／底片框／入場／換場／結尾／字幕粗細）全部替換。細節：

- 清單用 `position:fixed`，不會被面板捲動區裁掉
- 寬度 `max-content` 貼合最長項目（原本綁死按鈕寬度會把「4:5 直式 1080×1350」切掉）
- 下方空間不夠會自動往上開
- 鍵盤可用（上下鍵／Enter／Esc）
- 濾鏡鎖定時變灰且點不開（MutationObserver 同步 `disabled`）

**修掉兩個 bug**：開新選單不會關舊的（連開五次疊五張）；`e.target` 不是元素時 `contains()` 會丟 TypeError 讓關閉邏輯失效。

---

## 17 · 輸出鐵盒

右下角的銀盒取代原本的輸出按鈕。收起時只露一角、角度 9°；hover 滑出並轉正，顯示 **tooltip**。

**tooltip 的樣式**：深色反白（墨底米白字、無框線）。淺底加框線在這個介面裡是按鈕與輸入欄位的語彙，會讓人以為可以按；深色實心無框才讀得出是「暫時浮出的說明」。

**點擊穿透的修正**：鐵盒是 `pointer-events:none`（才不會擋住底下的面板），所以事件其實打在底下的元件上，點鐵盒會同時展開底下的下拉選單。原本只在 `click` 攔截，但**原生 select 是在 `pointerdown` 就展開的**，來不及。改成在捕獲階段就攔下 `mousedown` / `pointerdown` / `click`。

---

## 18 · 素材列

- 新增多張素材維持一排、可左右捲動，`+` 釘在右端
- 移除左側的漸層提示（素材不多時反而是雜訊）

---

## 19 · 資源瘦身

| 檔案 | 之前 | 之後 | |
|---|---|---|---|
| tracing-paper-1 | 247 KB | 63 KB | 25% |
| tracing-paper-2 | 492 KB | 123 KB | 25% |
| tracing-paper-3 | 2,435 KB | 513 KB | 21% |
| 取景匡 | 1,784 KB | 172 KB | 10% |
| **assets/ 合計** | **3.4 MB** | **1.3 MB** | |

全部用 WebP q94，可見誤差（色差乘上該像素不透明度）平均 0.33～0.45／255。

**一個重要發現**：WebP 就算開有損模式，**alpha 通道也是無損編碼**的 —— 實測 alpha 誤差 0。所以描圖紙 3（同時當 `.pmask` 的遮罩用）與取景器的透明度完全沒被動到。

---

## 20 · 素材搬到中間畫板（2026-09-18）

- 素材縮圖與「＋」從左欄移到中間畫板、成品正下方；左欄只剩操作
- 左欄那格改名「規格 FORMAT」，留下比例與底片框
- 「清除全部素材」縮成素材列右端的小按鈕「清除」
- 版面：新增 `.board` 包住成品與素材列，兩者同寬；
  成品的高度預算多扣一個 `--barh`（74px）給素材列，短視窗才不會被擠出去
- 素材列不在 `#frame` 裡，所以不會被畫進輸出影片

---

## 21 · 方寸之間：只有相機在晃（2026-09-18）

- 原本 `handHold` 晃的是 `rig`（機身＋窗＋照片一起動），看起來像整個世界在晃
- 改成照片用一層 `.hold` 反向位移抵銷掉，變成「景站著不動、相機在飄」
- 為什麼不是「只晃機身」：洞跟窗的對位是量 alpha 量出來的（padx/pady 那一圈），
  拆開晃會讓洞漂出窗外、露出窗的硬邊
- 反向量有上限：模擬 10 分鐘，水平位移 p50=0.94、p99=4.53、max=8.24 cqw，
  而窗超出洞的餘裕只有 4.6×size ≈ 5.3%，硬抵銷會爆掉 →
  用 `tanh` 軟性收邊，上限＝那一圈＋照片放大溢出的部分，再取 80% 當安全係數
- 小幅度幾乎原樣抵銷（p50 只差 2%），晃很大時照片才跟著帶一點，不會有硬邊
- 已確認輸出路徑吃得下：`paintTree` 會遞迴套每一層的 transform，
  預覽 canvas 量到窗內平均亮度 135（不是黑的）

---

## 22 · 濾鏡清單收起說明（2026-09-18）

- 說明文字平常收起來，只留名字；hover 或鍵盤 focus 才展開
- 11 個濾鏡從要捲動變成一頁看完
- 用 `grid-template-rows:0fr→1fr` 做高度動畫，不是 `max-height` ——
  後者要猜一個值，猜太小會切掉、猜太大收合速度會不對

---

## 23 · 濾鏡清單改成縮圖卡片（2026-09-18）

- 兩欄網格，上面是套好濾鏡的樣張、下面是名稱（Lightroom 那種排法）
- 樣張用 Lily 的狗狗照片：`assets/preset-sample.webp`（400×500、q94、91 KB），
  原圖留在 `source/濾鏡樣本_狗狗.jpg`（3025×3781）
- **縮圖不是預先算好的圖檔**，是拿同一張照片、當場跑一次該濾鏡的 `build()` 長出來的，
  跑完立刻 `T.clear()`，只留建好的那一格 → 靜止、不吃 CPU、而且永遠等於實際效果
- 代價：純靠計時器才看得到的東西（漏光閃一下、刮痕、塵點）不會出現在縮圖裡，
  縮圖只呈現該濾鏡的底色調
- `setVar` 改成可切換寫入對象（`varTarget`）。原本固定寫到 `#frame`，
  十個縮圖各自 `update()` 會互相蓋掉主畫面的變數；建縮圖時暫時指到縮圖盒子，建完還原
- `mediaEls` 也要備份還原 —— 那是輸出時要等的素材清單，混進縮圖的圖會出事
- 縮圖盒子要有 `container-type:size`，濾鏡內部全是 `cqw` 單位
- 說明文字改成壓在樣張下緣、hover 才浮出的字幕條（取代第 22 章的展開式），
  這樣不會把網格撐開

---

## 24 · 手機版重做成三段式（2026-09-18）

原本手機版是把三欄直接攤平成一欄，畫板被擠到第二屏，完全不能用。改成：

| 段 | 內容 | 行為 |
|---|---|---|
| 上 | 主畫板 + 素材 | 固定不動 |
| 中 | 濾鏡縮圖 | 一排左右滑 |
| 下 | 規格 / 微調 / 字幕 / 放映 | 只有這段上下捲 |

- 捲設定的時候畫板永遠看得到（實測：`panel-r` 捲到底，`.stage` 的 top 沒有變）
- `規格` 那格從左欄搬到右欄最上面 —— 左欄只留濾鏡，手機才切得出「固定區／捲動區」。
  桌機因此變成左欄＝濾鏡、右欄＝規格＋微調＋字幕＋放映
- 手機上 `#cellParams`（微調）用 `order:-1` 排到最前面：剛選完濾鏡最想動的是它
- `body` 在手機改成直向 flex，就不用去猜頁首多高（它會隨字級變）。
  其他 body 的孩子（蕾絲、Our Story、銀盒、輸出面板）都是 `position:fixed`，不受影響
- 高度一律用 `dvh` 不用 `vh`：手機網址列縮起來時 `vh` 不會變，畫面會被切掉
- 畫板 `32dvh`、濾鏡卡 66px，390×844 上設定區還有約 290px 可捲
- 濾鏡那格的 `− 效果` 可以點掉收起來，要更多空間調參數時很好用

<details>
<summary>驗證踩到的坑：headless Chrome 量不到 390px</summary>

`--window-size=390,844` 截出來的圖是 390 寬沒錯，但頁面裡 `innerWidth` 其實是 **500** ——
headless 的視窗有 500px 下限，所以畫板看起來偏右、像是版面爆寬。
改成「用一個 390px 的 iframe 載入頁面」再截圖才是真的手機寬度，量到 `docScrollW=500=viewport`，
沒有任何橫向溢出。
</details>

---

## 25 · 手機版銀盒與濾鏡順序（2026-09-18）

- 手機上的輸出銀盒放大到 `62vw`、往中間移，可見中心從畫面最右邊拉到 x≈215（視窗中心 195）
- **位置只能用 `transform` 移，不能改 `right`/`bottom`** ——
  點擊判定 `onIron()` 是把視窗右下角當原點、再把 computed transform 反推回去算的，
  改 `right`/`bottom` 判定會整個歪掉；改 `transform` 它會自己跟上
- 設定區補 `padding-bottom:96px`，最後一排才不會被銀盒蓋住
  （實測：捲到底最後一列底部 y=721、銀盒頂端 y=752，沒被蓋到）
- 濾鏡順序改用獨立的 `PRESET_ORDER` 清單決定，不動 `PRESETS` 的定義順序
  （那幾塊很大，搬來搬去容易出錯）。沒列到的會自動接在後面，新增濾鏡不會憑空消失
- 新順序：時間光漏 / 陽光褪色 / 金色那天 / Super 8 / 8mm 放映機 /
  花田時光 / 褪色暖調 / 霧裡微亮 / 方寸之間 / 黑白老電影

---

## 26 · 手機版：輸出鈕與素材直排（2026-09-18）

**銀盒改成底部輸出鈕。** 銀盒要 hover 才展開、而且靠幾何判定點擊，手機沒有 hover，
只會變成「看不懂要幹嘛又很難按的裝飾」。手機直接 `display:none`，
換成釘在最下面的一顆 52px 全寬按鈕，共用同一個 `open()`，面板／預覽／下載流程完全一樣。

- 按鈕亮暗寫在 `open()` 裡而不是按鈕的 click —— 這樣「點面板外面關掉」時狀態也會還原
- `pointerdown` 的「點到外面就關掉」要把 `#exportBar` 排除，
  否則 pointerdown 先關、click 又開，會變成關不掉
- 輸出面板改成 `left/right:12px`、`bottom:72px`，浮在按鈕上方

**濾鏡一路兩欄到手機版為止。** 原本 ≤1180px 會掉成一欄，變成一張超大圖要一直捲，反而更難挑；
現在只在 ≤1280px 把間距收窄成 12/8px。900px 寬時縮圖約 93px，兩欄十個一頁看完、不用捲。

**素材改成直排、放在畫板左邊。** 原本在畫板下方，會吃掉高度 —— 而手機最缺的就是高度；
放左邊吃的是寬度，4:5 的畫板本來就用不完整個寬度。

- `.board` 在手機變成橫向 flex，`.stagebar{order:-1}` 排到左邊（DOM 上它在畫板後面）
- 畫板 `flex:1`，高度由 `aspect-ratio` 自己長，螢幕再窄也只是等比縮小、不會被壓扁
- 「＋」從「釘在橫排最右邊」改成「釘在直排最下面」
- 畫板從 270×216 變成 **350×280**（高 +30%）

<details>
<summary>390×844 實測</summary>

| 區塊 | 位置 | 高 |
|---|---|---|
| 素材欄 | x 22..78（左） | 350 |
| 畫板 | x 88..368 | 350 |
| 濾鏡條 | y 436..586 | 150 |
| 設定區 | y 586..834 | 249（內容 983，可捲） |
| 輸出鈕 | y 782..834 | 52（fixed） |

設定區捲到底，最後一列底部 y=734 < 輸出鈕頂端 782 → 不會被蓋住。
</details>

---

## 27 · 郵票濾鏡（2026-09-18）

同一張照片用兩次：放大模糊鋪底，中間壓一張齒孔郵票，票面裡是清楚的那張。

- **齒孔是「一圈跟票面同色的實心小圓、各凸出去一半」做的，不是 mask-image 或 clip-path。**
  那兩個輸出用的 canvas 渲染器都不支援（見 README 的第一條重點），
  但它畫得出「有 border-radius 的色塊」（`radiusOf` 會把 50% 換算成半徑），
  所以走圓點這條路，預覽跟輸出才會長一樣
- 橫邊 10 顆、直邊 13 顆（票型寬高比 0.78，`ST_AR` 與 CSS 的 `.78` 要一致），共 46 顆，
  全部用百分比排 → 郵票縮放時齒孔自動等比縮
- 郵票大小用 `min(size*1cqh, 84cqw/.78)`：高的畫面吃高度、扁的畫面（16:9）改吃寬度上限，
  取小的那個才不會被壓扁或爆出畫面
- 票面下緣留寬一點（`bottom:13%` vs `top:6%`），郵票本來就長這樣
- 背景那張用 `cloneNode` 而不是再呼叫一次 `makeMedia` —— 否則影片的 ended 事件會註冊兩次，會連跳兩張素材（跟方寸之間同一個坑）

---

## 28 · 輸出多一個「下載圖片」（2026-09-18）

- 面板變成兩顆按鈕：下載影片 / 下載圖片
- 下載圖片 = 每個素材各出一張 PNG（四個素材就四張），檔名 `i-saw-<濾鏡>-01-1080x1350.png`
- 走的是跟影片同一條渲染路徑（`drawFrame`），所以圖片 = 影片裡的那一格 = 預覽
- 作法：依序把畫板切到每一個素材 → `awaitMedia()` 等它可繪製 → 畫進 canvas → 存檔 → 最後切回原本那張
- **先空畫 4 格暖機**：顆粒、漏光、塵點那些層是逐格長出來的，
  第一格畫出來還是乾淨底圖，不是那個濾鏡真正的樣子
- **每張之間隔 320ms**：連續觸發下載瀏覽器會把後面的當成彈出視窗擋掉
- 實測 2 個素材 → 2 張 1080×1350 PNG，1.6 MB / 2.2 MB，平均亮度 118 / 99（不是空白）

---

## 29 · 郵票重做（2026-09-18）——取代第 27 章

第 27 章那版是用 CSS 圓點自己拼齒孔，**做出來很醜**：我把齒孔做成往外凸的花邊，
真正的郵票是往內咬的圓凹口，完全是兩回事。整個換掉。

- 改用 Lily 畫的透明 PNG：`assets/stamp-frame.webp`（1102×1428，180 KB，
  由 `source/郵票匡.png` 轉出，`-alpha_q 100`，實測 alpha 誤差 0）。
  跟「方寸之間」的取景器同一個做法：圖直接疊上去，中間的洞是透明的
- 量測（自 alpha 通道）：透明窗 x 15.25%~85.03%、y 11.83%~87.82%（佔 69.78%×75.98%）；
  郵票實體 x 7.44%~92.74%、y 6.02%~93.77%；圖檔比 0.7717
- 照片窗外擴 1.2% / 1.0%，讓照片的硬邊壓在不透明的框底下。
  內框是實心紅線（#5D0405）不是羽化的，所以不用像取景器那樣外擴那麼多

**行為：第一張當底圖，後面的在郵票裡輪播。**

- 濾鏡多一個旗標 `bgFirst`。搭配兩個新的 helper：
  `firstIndex()`（輪播從第幾張開始）、`stepIndex()`（下一張是第幾張）
- `nextScene` 的三條換場分支、`resumeFromEnd`、輸出影片的起點、`totalSecs`、
  輸出圖片的迴圈範圍，全部改走這兩個 helper。其他濾鏡 `firstIndex()` 一律是 0，行為沒變
- 索引在 1..n-1 之間繞，永遠不會回到 0 —— 底圖那張不會跑進小框裡
- 選到郵票時若索引停在 0 會自動跳到 1，否則小框會跟底圖同一張
- 底圖那張用自己做的 media，**不註冊影片的 ended 事件**（那是輪播那張的事，
  註冊兩次會連跳兩張 —— 跟方寸之間同一個坑）

---

## 30 · 輸出可以取消（2026-09-18）

- 輸出中那顆按鈕就地變成「取消輸出」（紅框），另一顆停用；倒數也會提示可以按取消
- `busy` 從布林改成 `false / 'video' / 'still'`，配 `cancelReq` 旗標
- 影片：每格檢查 `cancelReq` → `rec.stop()`；`onstop` 看到取消就只收尾、不存檔
- 圖片：每張開始前檢查，取消後回報「已取消，已經存了 N 張」
- 實測（用 MediaRecorder 替身跑流程）：
  錄影中按鈕 = 「取消輸出」+ 紅框、圖片鈕停用、狀態「還有 15.9 秒（可以按取消）」；
  按下取消後 → 「下載影片」、狀態「已取消，沒有存檔。」

<details>
<summary>沒驗到的部分</summary>

「取消輸出圖片」這條路徑沒有實際跑過 —— cmux 內建的瀏覽器（WebKit）`toBlob` 會擋，
輸出圖片一開始就失敗，沒機會按取消。它跟影片共用同一套 `cancelReq` / `setBusyUI`，
但我沒有實測。Chrome 下載圖片本身是正常的（Lily 已經存到 3 張）。
</details>

---

## 31 · 郵票改成底片框（2026-09-18）——取代第 29 章

郵票不是濾鏡，是**底片框** —— 這樣才能跟任何濾鏡疊。第 29 章那個 `PRESETS.stamp` 整個拿掉。

- `底片框` 下拉多一個「郵票」，圖檔走 `assets/stamp-frame.webp`
- **兩格**：`MASK_SLOTS['stamp-4x5']=[[0,0,100,100],[14.05,10.83,72.18,77.99]]`
  第一格滿版（框外面的底圖，固定第一張照片），第二格是票面的窗（第二張之後輪播）
- 選郵票框就把比例鎖在 4:5（跟方寸之間一樣的做法），切走時原封不動還回去。
  郵票圖是 0.7717 直式，拉到 16:9 齒孔會變扁圓
- `firstIndex()` 從「看濾鏡」改成「看底片框」：`maskState.id==='stamp'` 就從第 1 張開始輪播

<details>
<summary>兩個踩到的坑</summary>

**底圖那格不能自己寫 filter。** 一開始底圖是獨立一層 `.stampbg`，自己套 `blur + brightness`，
結果把濾鏡的 `filter` 整個蓋掉 —— 框外面是原本的綠、框裡面是金色，顏色分家。
改成讓底圖也走同一個濾鏡的 `build()`（所以才要做成「兩格」），
放大只用 `transform`、壓暗用 `::after` 蓋一層半透明黑 ——
這兩個輸出的 canvas 渲染器都畫得出來，但它**不支援「父層 filter 影響子層」**，所以不能把 filter 放在容器上。

**`.slot:first-child` 選不到。** `#frame` 裡前面永遠有 `flick / trans / fade / intro` 四個固定層，
格子不可能是第一個孩子。改成在 `build()` 掛一個 `.bgcell` class 來認。
</details>

**實測（輸出預覽，走的是影片輸出同一條渲染路徑）**：框的白邊亮度 222、窗中央 122、
框外底圖 112、四角 27（暗角）—— 框、底圖、壓暗層輸出都畫得出來。

---

## 32 · 郵票框去背與縮小（2026-09-18）

**紅邊是原本的紅色背景沒去乾淨。** 量出來：內外緣各有一條 6~9px 的暗紅帶（#5F0404 附近），
而且 alpha 幾乎是滿的 —— 也就是去背時把背景像素當成郵票留下來了。
外緣那條尤其明顯不是設計（真的郵票不會沿著齒孔描一圈紅線）。

- 用**反推混色**處理，不是硬砍顏色：
  假設 `看到的 = f × 米色 + (1−f) × 底色紅`，用綠色通道解 `f`（米色 G=221、紅 G=4，分得最開），
  然後 `新 alpha = 原 alpha × f`、`新顏色 = (看到的 − (1−f)×紅) / f`
- 這樣邊緣是平滑收掉的，不會留下一圈鋸齒或深色描邊
- 結果：明顯偏紅的像素 **51452 → 12**
- 去背後重量：透明窗 x 14.43%~85.93%、y 11.20%~88.52%（紅帶拿掉後窗變大一點）
- 原圖留 `source/郵票匡.png`，去背版 `source/郵票匡_去背.png`

**縮小到 70%。**

- `STAMP_SCALE=.7` 這個常數**同時**決定槽位與框圖的尺寸 ——
  改一個地方兩邊一起動，不會對不上
- 框圖用 inline style 縮到 70% 置中（`.masklayer` 本來是滿版的）
- 槽位由 `STAMP_WIN`（量到的窗，四周各外擴約 1%）依同一個比例換算：
  `[24.40, 22.28, 51.45, 55.24]`
- 實測輸出預覽：窗的上下左右四個內緣都是照片（沒有露出黑底），
  框的米白 226,207,195、框外底圖 214,199,185

---

## 33 · 郵票框不再鎖比例（2026-09-18）——修正第 31 章

第 31 章把比例鎖在 4:5 是想錯了。正確的是**比例照樣可以隨便換，郵票自己維持固定的形狀**
（跟「4:3 橫幅單格」一樣，每個比例都能用）。

- 拿掉 `applyMaskLock()` 與那個鎖
- 郵票的方框改成依當前比例算：
  `wf = SCALE × min(1, 郵票比/畫面比)`、`hf = SCALE × min(1, 畫面比/郵票比)` ——
  誰先撐到 70% 就以誰為準，另一邊按比例縮，所以**郵票不會被拉扁也不會爆出畫面**
- 槽位（票面的窗）跟著同一個方框換算，不再寫死在 `MASK_SLOTS` 裡
- `比例` 選「跟素材一樣」時，直接讀素材的原生比例來算，不是抓最接近的那一檔

**實測四種比例下郵票的實際寬高比**（應該都是 0.7717）：

| 比例 | 畫面 | 郵票寬高比 | 郵票佔畫面 |
|---|---|---|---|
| 4:5 | 332×415 | 0.7717 | 67.5% × 70.0% |
| 9:16 | 332×590 | 0.7717 | 70.0% × 51.0% |
| 1:1 | 332×332 | 0.7717 | 54.0% × 70.0% |
| 16:9 | 332×187 | 0.7718 | 30.4% × 70.0% |

---

## 34 · 修正：刪素材再新增，郵票前後景會變同一張（2026-09-18）

**症狀**：郵票框下刪掉一個素材、再新增一個，第一張會變成前景（票面）跟背景同一張照片。

**原因**：`state.index` 被歸 0 了。增減素材的地方各自寫了「超出長度就歸 0 / 退到最後一張」，
但郵票框的輪播是從第 1 張開始的（第 0 張固定當背景），索引一旦停在 0，
票面的窗就會拿到第 0 張 —— 跟背景同一張。

**修法**：新增 `clampIndex()` 統一把索引拉回合法範圍，四個地方都接上：
`removeItem`、`addFiles`、換底片框、換濾鏡，最後在 `build()` 再收一次當保險 ——
不管從哪條路徑進來，索引一定是合法的。

**實測**（郵票框，每一步都看背景跟票面是不是同一張）：

| 步驟 | 索引 | 前後景 |
|---|---|---|
| 三張素材 | 1 | 不同 ✓ |
| 刪掉第 2 張 | 1 | 不同 ✓ |
| 再刪到剩一張 | 0 | 同一張（**只有一張素材時本來就只能這樣**） |
| 刪完再新增一張 | 1 | 不同 ✓ ← 原本壞在這 |
| 再新增一張 | 1 | 不同 ✓ |

---

## 35 · 蕾絲邊框 ＋ 圖框機制通用化（2026-09-18）

照郵票的邏輯加了蕾絲框。順手把郵票那套寫死的程式抽成一張表 —— **以後要再加框只要加一列**。

```js
const FRAMES={
  stamp:{src:'assets/stamp-frame.webp', ar:1102/1428, scale:.70, win:[13.43,86.93,10.40,89.32]},
  lace: {src:'assets/lace-frame.webp',  ar:1056/1489, scale:.94, win:[19.94,80.06,13.91,84.68]}
};
```

原本散在六個地方的 `maskState.id==='stamp'` 判斷全部改走 `frameMask()`，
`stampBox/stampSlots` 改成 `frameBox/frameSlots`。

**蕾絲圖的量測**（`source/蕾絲邊框.png`，1056×1489，比例 0.7092）：

- 中央透明窗 x 20.74%~79.26%、y 14.51%~84.08%，**五條掃描線量下來都一樣，是乾淨的矩形**
- 蕾絲實體 x 2.56%~97.44%、y 1.48%~98.25%
- 沒有去背殘色（色偏很大的不透明像素 0 個，本體 R253 G247 B236）——
  跟郵票那張不同，這張不用修
- 中央區完全透明，沒有把浮水印壓進去（預覽看到的格子是透明背景的棋盤格）
- `scale` 給 .94（郵票是 .70）：蕾絲本來就是貼著邊的花邊，縮太小會變成「畫中畫」

**檔案大小**：`assets/lace-frame.webp` 756 KB（q92 + alpha 無損）。
試過降到 q78 只省到 594 KB —— 檔案大小是被 alpha 吃掉的（蕾絲的鏤空非常碎），
降畫質換不到什麼，所以維持 README 說的「alpha 一律無損」。
這張只有選到蕾絲框時才會載，不影響一般使用。

**實測（兩個框 × 四種比例）**：實際寬高比全部等於圖檔比（郵票 0.7717、蕾絲 0.7092），
前景與背景永遠是不同的素材。

| 框 | 4:5 | 9:16 | 1:1 | 16:9 |
|---|---|---|---|---|
| 郵票 | 68%×70% | 70%×51% | 54%×70% | 30%×70% |
| 蕾絲 | 83%×94% | 94%×75% | 67%×94% | 37%×94% |

---

## 36 · 蕾絲兩格 ＋ 圖框支援多個窗（2026-09-18）

- `FRAMES` 的 `win` 改成 `wins`（陣列），一個框可以有很多個窗，照順序輪播
- 新增 `cycItem(k)`：在「扣掉底圖那張」的範圍內取第 k 張，兩個窗就是 k 與 k+1
- 選單：原本的「蕾絲」改名「蕾絲一格」，新增「蕾絲兩格」

**量測（`~/Downloads/蕾絲2.png`，1024×1536）**

- **先裁掉四周空白**：蕾絲實體只佔 x 16.02%~83.89%、y 5.47%~94.47%，
  左右各有 16% 是空的。不裁的話 `scale` 是算在一張大半是空白的畫布上，框會比預期小很多。
  裁完 **702×1375（比例 0.5105）**
- 兩個窗（裁切後、已外擴 0.8% / 0.6%）：
  上窗 `[26.27,73.59,10.82,47.29]`、下窗 `[26.27,73.59,49.95,84.45]`
  五條掃描線量下來左右內緣都是 27.07%/72.79%，是乾淨的矩形
- **預覽看到的黑塊與紅黃邊是假的**：實際資料裡窗的區域 alpha 全 0（乾淨），
  彩色雜訊整張只有 2 個像素。那是預覽把透明畫成黑色造成的錯覺，所以沒有做任何去背修補
- 蕾絲本體的 alpha 大約 208~235（不是 255）—— 這張蕾絲本身就是半透明的網紋，
  所以背後的底圖會透出來一點，是它原本的樣子

**實測輪播**（4 張素材：bg + p1/p2/p3）：

| | 底圖 | 上窗 | 下窗 |
|---|---|---|---|
| 起始 | bg | p1 | p2 |
| 換一次 | bg | p2 | p3 |
| 再換 | bg | p3 | p1 |
| 再換 | bg | p1 | p2 |

底圖永遠是第一張、兩個窗永遠不會撈到它；框的實際寬高比 0.5105 = 圖檔比 0.5105。

---

## 37 · 蕾絲一格／兩格換新素材（2026-09-18）

兩張都換成新版（都是 1024×1536，一格那張改成有 LILY 牌的版本）。重新裁切、重新量、重新轉檔。

| | 裁切後 | 比例 | 窗 |
|---|---|---|---|
| 蕾絲一格 | 913×1253 | 0.7287 | `[13.88,86.12,13.61,85.76]` |
| 蕾絲兩格 | 626×1356 | 0.4617 | `[19.17,81.47,9.87,47.80]`、`[19.17,81.47,50.06,89.54]` |

- **預覽上那一片黑點又是假的。** 四個角落量下來不透明像素 **0 個**，
  兩張都是乾淨的 RGBA。是預覽把透明畫成黑點造成的，所以一樣沒做任何去背修補
- 兩張原檔左右都有大片空白，一樣先裁到內容範圍才用
- 左右內緣五條掃描線量下來完全一致（一格 14.68%、兩格 19.97%），確認是乾淨的矩形

**實測（兩個框 × 三種比例）**：實際寬高比全部等於圖檔比，沒有被拉變形；
一格 = 底圖+1 窗、兩格 = 底圖+2 窗，素材分配正確。

| 框 | 4:5 | 9:16 | 16:9 |
|---|---|---|---|
| 蕾絲一格 | 86%×94% | 94%×73% | 39%×94% |
| 蕾絲兩格 | 54%×94% | 77%×94% | 24%×94% |

---

## 38 · 蕾絲素材再更新一次（2026-09-18 12:53）

兩張又換了新版，重跑一次同一套流程（檢查乾淨度 → 裁切空白邊 → 多線量窗 → 轉 WebP）。

- 幾何幾乎沒變：一格 913×1252（0.7292）、兩格 626×1356（0.4617）
- 兩張都乾淨（四角落不透明像素 0、彩色雜訊 0），一樣不用去背
- **一格那張的 WebP 從 686KB 掉到 336KB** —— 新原檔本身乾淨很多（調色盤只用了 129 色）
-  總計 3.1 MB → 2.8 MB

---

## 39 · 兩張蕾絲框縮小 30%（2026-09-18）

`FRAMES.lace` / `FRAMES.lace2` 的 `scale` 從 `.94` 改成 `.66`（0.94 × 0.7）。
郵票維持 `.70` 沒動。

`scale` 一個數字同時決定框圖尺寸與槽位，所以窗會跟著等比縮、不會跑掉。

---

## 40 · 框外底圖的壓暗改成各框自己決定（2026-09-18）

蕾絲的底圖不壓暗（原本兩種框共用同一組壓暗設定）。

- `FRAMES` 多一個 `dim`：郵票 `.45`、蕾絲兩張都 `0`
- 建格子時把它寫成 CSS 變數掛在底圖那格上，`::after` 讀它

<details>
<summary>踩到兩個坑</summary>

**1. `setProperty` 寫在 `cssText` 前面會被洗掉。**
`root.style.cssText = ...` 是整段覆蓋，不是附加。變數必須寫在它後面。

**2. `setProperty` 的值一定要是字串。**
傳數字（`0.45`）WebKit 會直接忽略，靜靜地什麼都不做。要 `String(...)`。

這兩個加起來的症狀一模一樣：變數讀出來是空字串、壓暗層永遠是預設值。
</details>

**實測**：郵票 `rgba(10,8,6,0.45)`、蕾絲一格與兩格都是 `rgba(10,8,6,0)`（完全透明），
底圖那格的槽位仍然是 100%×100%（沒有被改順序影響）。

---

## 41 · 星星對切（2026-09-18）

畫面上下對切，兩半放同一個素材各播各的；同一張星星遮罩用兩次 ——
上半「星星填上顏色蓋在照片上」，下半「整片顏色、星星處挖空露出照片」。

**為什麼是濾鏡不是底片框**：顏色要能換。底片框在這個程式裡一個可調欄位都沒有，
要有「微調」面板就只能是濾鏡。

**關鍵作法：用 canvas 合成，不用 mask-image。**
輸出影片的渲染器不支援 `mask-image` 與 `clip-path`（我查過 `paintTree`：
它只吃 backgroundColor / backgroundImage / borderRadius / boxShadow / filter /
mixBlendMode / opacity / overflow ＋ transform ＋ 偽元素）。
直接寫會變成預覽漂亮、輸出是色塊。

所以改成載入遮罩圖後用 canvas 合成兩張圖，再當 `<img>` 用：

- `destination-in` → 只留下遮罩不透明的地方 = 星星本身（ink）
- `destination-out` → 把遮罩不透明的地方挖掉 = 星星以外的整片（hole）

結果依「顏色 + 模式」快取，拉滑桿不會重畫。

**上下對齊**：兩半的遮罩層盒子一樣大、都用 cover，所以星星位置完全對齊。

**素材目前是暫用的**：`tools/genstars.py` 產生的 16 顆十角星（固定亂數種子，每次一樣）。
Lily 的正式遮罩進來後只要覆蓋 `assets/stars-mask.webp`，程式一行都不用改。

**實測輸出預覽**：上半奶油色像素 616、下半 4197（約 6.8 倍）——
下半是大面積底色、上半只有零星星星，正反關係正確，預覽 = 輸出。

---

## 42 · 星星對切鎖住底片框（2026-09-18）

這個濾鏡自己就是在切版面，再疊一個底片框會打架。選它就把底片框固定成「不用遮罩」並反灰。

**順手把鎖改成可以分項。** 原本 `applyPresetLock` 是「有 lock 就兩個一起鎖」，
但星星對切只需要鎖底片框、比例照樣可以換。改成：

- `方寸之間  lock:{aspect:'0.8', mask:''}` → 兩個都鎖
- `星星對切  lock:{mask:''}` → 只鎖底片框

作法是先把上一個濾鏡鎖住的東西原封不動還回去，再套這個濾鏡的鎖；
`lockSaved` 只記「這次真的有鎖的那幾項」，沒鎖的項目完全不會被動到。

**實測**（從「9:16 + 蕾絲一格」開始）：

| 步驟 | 比例 | 比例鎖 | 底片框 | 底片框鎖 |
|---|---|---|---|---|
| 原本 | 9:16 | — | 蕾絲一格 | — |
| 選星星對切 | 9:16（沒動） | 沒鎖 | 不用遮罩 | **鎖住** |
| 切到金色那天 | 9:16 | 沒鎖 | 蕾絲一格（還原） | 沒鎖 |
| 切到方寸之間 | 4:5 | **鎖住** | 不用遮罩 | **鎖住** |
| 再切回金色那天 | 9:16（還原） | 沒鎖 | 蕾絲一格（還原） | 沒鎖 |

---

## 43 · 「底片框」改名「載體」（2026-09-18）

清單裡已經有觀景窗、郵票、蕾絲，都不是底片框了；「載體」比較涵蓋得住
——它們的共同點是「承載照片的那個東西」。

畫面上只有一處標籤，其餘 11 處是程式註解，一起改掉免得以後看了混亂。
`#maskSel`、`maskState`、`MASK_SLOTS` 這些內部名稱沒動（改了要牽動很多地方，
而且不影響使用者看到的東西）。

第一個選項「不用遮罩」改成「無」。README 裡的三處也一起改了。
資料夾名稱 `masks/` 沒動 —— 那是給 Lily 自己看的，改了還要牽動程式裡的路徑。

---

## 44 · 郵票載體背景也不壓暗了（2026-09-18）

`FRAMES.stamp` 的 `dim` 從 `.45` 改成 `0`，跟蕾絲兩張一樣。
三個圖框類載體現在都不壓暗框外面的底圖。

實測：`--sbdim` 變數 = `0`、`::after` 壓暗層 = `rgba(10,8,6,0)`（完全透明）。

---

## 45 · 今天的小星星：橫式改左右切（2026-09-18）

直式／方形（4:5、9:16…）維持上下對切；**寬≥高（16:9 那類）自動變成左右對切**。

- 用 `@container (min-aspect-ratio:1/1)` 讀 `.frame` 自己的長寬比（`.frame` 本來就有
  `aspect-ratio:var(--ar)` 且 `container-type:size`），不用 JS 判斷比例、也不用另外監聽
  比例切換的事件 —— 畫面比例一改就自動切換，連「跟素材一樣」這種算出來的比例也一樣有效
- 輸出用的 canvas 渲染器讀的是最終算好的版面位置（`offsetLeft/Width`…），
  跟這個版面是「一般 CSS 規則」還是「容器查詢」決定的無關，所以預覽跟輸出影片會是同一個版面
- 星星圖層本來就是 `object-fit:cover`（跟照片同一條規則），版面從「寬矮」變「窄高」
  不會把星星拉扯變形，只是改成裁切

**實測四種比例**（`getBoundingClientRect`）：

| 比例 | 切法 | 上/左格 | 下/右格 |
|---|---|---|---|
| 4:5 | 上下 | top 185, 366×229 | top 414, 366×229 |
| 1:1 | **左右**（門檻剛好含等於） | left 310, 183×366 | left 493, 183×366 |
| 16:9 | 左右 | left 310, 183×206 | left 493, 183×206 |
| 9:16 | 上下 | top 103, 350×311 | top 414, 350×311 |

輸出預覽也驗過：16:9 時左半奶油色像素 500、右半 5575，左右分佈明顯不同（不是上下）。

---

## 46 · 精裝記憶：第一個「滿版裁切」型載體（2026-09-18）

跟郵票／蕾絲不一樣的地方：**這張本身就是滿版設計**（整張不透明的暗紅底＋紙角＋蕾絲角，
中間挖一個窗），不需要另一張底圖補周圍。照 Lily 的話「固定比例，不同尺寸就靠裁切」，
新增 `mode:'cover'`：框圖永遠鋪滿整個畫面，跟畫面比例不合的地方直接裁掉（不是縮小置中）。

**量測**（`~/Downloads/精裝記憶.png`，1131×1772，比例 0.6383，整張圖 0%~100% 都是滿的，
不用像蕾絲那樣先裁空白邊）：

- 窗（乾淨矩形，避開左上紙角與右下蕾絲咬進來的地方）：`x 27.06%~72.86%  y 30.25%~68.85%`
- 邊緣是硬切、沒有羽化（相鄰兩個像素直接從全不透明跳到全透明），不用外擴留餘裕

**關鍵技術決定：窗的位置要跟著「裁掉了多少」重新換算。**
`object-fit:cover` 在畫面比例跟圖檔比例不一樣時會裁切圖片；圖被裁掉一塊，
窗在畫面上的位置當然也要跟著挪，不然畫面比例一換窗就對不齊框了。
新增 `coverWinSlot()`，套用跟 `object-fit:cover` 完全一樣的裁切公式反推：
圖比畫面寬就是裁左右（算出還留著原圖的中間多寬），比畫面窄就是裁上下，
再把窗的座標從「原圖座標」換算成「裁剩下的那一段裡的座標」。

**確認畫面渲染器支援 `<img>` 的 `object-fit:cover`**：查了 `paintMedia()`，
它本來就有處理 `cover`／`contain`（算縮放倍率＋置中＋裁切），跟真的 CSS 行為一致，
所以這張走 `object-fit:cover` 預覽跟輸出影片會一樣，不用像星星對切那樣自己合成圖層。

**跟「底圖＋窗」機制的差異**（`FRAMES[id].mode==='cover'` 時）：
- `firstIndex()` 不跳過第一張 —— 沒有底圖格，第一張就正常輪播
- `build()` 只給一格（`frameSlots()` 回傳單一槽位），不做 `.bgcell`、不套壓暗變數
- 素材輪播規則跟一般載體一樣，不是郵票／蕾絲那種「固定跳過第 0 張」

**實測四種比例**（`getBoundingClientRect` 換算成的槽位）：

| 比例 | 窗（left/top/width/height） |
|---|---|
| 4:5 | 27.06% / 25.25% / 45.80% / 48.38% |
| 9:16 | 23.97% / 30.25% / 51.97% / 38.60% |
| 1:1 | 27.06% / 19.06% / 45.80% / 60.48% |
| 16:9 | 27.06% / **0%** / 45.80% / **100%**（極端比例下夾在 0~100 內，窗幾乎吃滿整個高度） |

**輸出預覽也驗過**：窗內是照片內容（隨素材而變）、框外是暗紅底（136,28,31）、
蕾絲角是米粉色（231,216,210）——跟預覽長一樣。

---

## 47 · 今天的小星星：橫式時遮罩自動轉 90 度（2026-09-18）

**先修正一個我自己算錯的地方**：上一輪推算橫式格子大小時誤用了直式的高度（用了 183×366），
重新用實測值（16:9 時每格實際是 **183×206**，長寬比 0.888）算過：
不轉的話 `cover` 只顯示得到遮罩寬度的 55.5%，兩側星星被裁掉一大片。

Lily 提的解法：**16:9 時把遮罩素材轉 90 度再用**。算過確實更好 ——
遮罩轉 90 度後長寬比從 1.6 變成 0.625（比格子 0.888 更瘦），
`cover` 改成裁掉一點上下（寬度 100% 都看得到），比裁掉四成寬度好多了。

- `starLayerURL()` 多一個 `landscape` 參數：true 時用 canvas 把遮罩整張轉 90 度再合成
  ink／hole 圖層（旋轉不會讓十角星看起來歪掉 —— 星星本身有旋轉對稱，只是整組換個方向散開）
- `paint()` 用跟切版面同一個門檻判斷（`currentAR()>=1`），版面切成左右的時候遮罩也跟著轉，
  兩邊保證一起換、不會有版面已經是左右切、遮罩還是原方向的中間狀態

**實測**：16:9 時遮罩圖層的自然尺寸從 1080×675（1.6）變成 675×1080（0.625）；
輸出預覽左右兩半的顏色分佈跟轉之前同一個量級（539 / 5500），旋轉沒有破壞原本的正反關係。

---

## 48 · favicon（2026-09-18）

原本完全沒設，瀏覽器分頁是預設的白紙圖示。用 Lily 給的銀質百合徽章圖，
縮出 32px／48px（分頁小圖）、180px（手機加到主畫面用的 apple-touch-icon），
`<head>` 裡加三個 `<link>`。png 本身有透明背景，縮圖沒轉檔（favicon 慣例用 png，
不像其他素材那樣轉 WebP）。

---

## 49 · favicon 換新圖（2026-09-18）

Lily 更新了 favicon.png（572×572，原本 732×732），重新縮出 32/48/180 三個尺寸覆蓋。
程式（<head> 的三個 <link>）沒動，只換底圖。

---

## 50 · favicon 再換一次（2026-09-18）

再更新一版（551×572，非正方形但接近，直接縮成方形，變形很小）。同樣覆蓋三個尺寸。

---

## 51 · 新增「無」（2026-09-18）

不套任何色調的效果，照片影片本人的樣子。放在清單最前面。

- 保留基礎微調：曝光、飽和、對比、顆粒、暗角、淡入長度 —— 跟其他濾鏡同一套滑桿介面，
  只是預設值全部是「沒有效果」（曝光 1、飽和 1、對比 1、顆粒 0、暗角 0）
- 主要用途：**單獨拿載體**（郵票、蕾絲、精裝記憶…）疊照片，不被額外上色。
  之前想這樣用只能挑一個色調最淡的濾鏡將就，現在有專門的選項

實測：預覽取樣顏色 (97,110,71) vs 原圖同一點 (95,108,70)，差在縮圖取樣誤差內，
不是被上色；換到郵票／蕾絲／精裝記憶都能正常疊上去。

---

## 52 · 敬請期待（Lily 專屬載體）（2026-09-18）

跟精裝記憶同一套機制（`mode:'cover'`，見第 46 章）、同一張畫布尺寸（1131×1772），
但「窗」不是挖空的洞，是**一張半透明的米卡**（alpha 約 65%，不是 0）——
照片會透過去，但永遠帶著卡片本身的米色跟裡面印的「coming soon」字。
這是設計本來就要的效果，我沒有把它當成瑕疵修掉。

**量測時的坑**：卡片中間印著「coming soon」文字，文字本身是不透明的（alpha=255），
從正中央往外掃會直接撞到文字就停下來，量出一個完全錯誤的（幾乎零寬）窗。
改成從幾條刻意避開文字的掃描線（y 35%/40%/55%/60%/65%）量，抓到一致的邊界：
`x 24.93%~75.07%  y 29.18%~70.77%`，四周再外擴一點點塞進緊貼著的那圈不透明白邊底下。

**取名**：這個載體 Lily 只說「這是我自己專屬的」，沒給名字，我用畫面上印著的字取成
「敬請期待」（內部代號 `soon`）。想換別的名字，改一個字串就好。

**實測**：卡片內取樣 (199,193,182) —— 照片跟米色混出來的顏色，不是純黑也不是純照片，
輸出渲染器（`ctx.drawImage` 本來就吃圖片自己的 alpha 通道）跟預覽一致，不用額外處理。

---

## 53 · 敬請期待 素材更新（2026-09-18）

Lily 換了新版（同尺寸 1131×1772），先確認窗的位置沒變（五條掃描線量下來
跟舊版數字一模一樣：x 24.93%~75.07%、y 29.18%~70.77%），所以程式參數不用動，
只覆蓋 assets/soon-frame.webp。新版拿掉了「coming soon」字，卡片也更透明一點。

---

## 54 · 紙上時光（新載體，2026-09-20）

跟郵票同邏輯（背景一張滿版、前景一張輪播），但**沒有一張手繪 PNG 決定前景形狀**——
前景是純白邊框，用 CSS 算出來的（像沖洗照片：四邊窄邊、下方留寬，像拍立得那排）。
描圖紙是唯一用到的圖檔，蓋在最上層統一調性。動手前先分析過，細節見下方摺疊。

<details>
<summary>動手前的分析：描圖紙的實際數據，為什麼不是「藍色濾鏡」</summary>

Lily 給的 `描圖紙.png`（1102×1500）量出來：
- **透明度**：整張 25%~63%（alpha 64~160），沒有全透明也沒有全不透明的地方 →
  設計來整張半透明覆蓋的材質，跟這個 app「Our Story」彈窗已經在用的描圖紙同一種概念
- **底色**：暖米色 R220/G210/B202，全圖一致
- Lily 問「藍藍der？」：量出來是真的，但很微弱——只有右側中段（x88-100%、y25-50%）
  R-B 從 19 降到 16，肉眼覺得「那裡有點灰藍」是因為整張紙太平、一點點差異就被放大感覺出來。
  **結論：這不是一張藍色的紙，是暖米色紙上有一小塊很淡的冷灰暈**，不足以支撐一個獨立的
  「藍色系」濾鏡，所以最後決定不獨立做濾鏡，只給這個新載體用（Lily 也確認了這個方向）
</details>

**跟前四個圖框類載體（郵票/蕾絲/精裝記憶/敬請期待）的關鍵差異**：那些都是「一張透明 PNG
決定形狀」，這個沒有形狀圖，`FRAMES.card` 的 `src` 是蓋在最上層的描圖紙，`ar`/`scale`
描述的是白邊卡片本身的大小（沿用 `frameBox()` 那套算法不用改），`photoInset:[6,6,6,22]`
是照片相對卡片四邊縮進多少 %（下方 22% 留寬，做出拍立得那排的感覺）。

新增 `mode:'polaroid'`：
- `frameSlots()` 多一個分支，算出「卡片外框」與「卡片內縮的照片窗」兩個槽位
- `applyMask()` 的 cover 定位判斷也把 polaroid 納入（描圖紙一樣要鋪滿整張畫面）
- `build()` 新增一塊 `.card-bg`（純 CSS 畫的白底＋淡陰影）——**踩到一個疊層順序的坑**：
  一開始把它插在整組格子迴圈「之前」，結果被滿版的底圖蓋掉，畫面整片黑。
  改成在底圖格（i===0）蓋完之後、照片窗（i===1）之前插入，靠 DOM 順序疊層，不用 z-index

<details>
<summary>驗證：另一個自己踩到的坑（跨網域測試失敗，誤判成程式壞了）</summary>

第一次寫測試頁放在 `/tmp/`、用 `file://` 開，裡面用 iframe 載入 `http://localhost:8765/...`——
這是跨網域，瀏覽器擋掉了 `contentDocument` 存取，畫面全黑，一度以為是程式邏輯錯誤在追。
後來確認是測試頁本身寫錯位置：**改成把測試頁存進專案資料夾、用同一個 `http://localhost:8765`
網域載入才正常**。之後同一個坑不要再犯。

修好之後實測：四種比例（4:5/9:16/1:1/16:9）卡片跟照片窗的比例關係都正確；
三張素材輪播，底圖固定第一張、照片窗在其餘素材間輪播；
輸出預覽取樣：照片窗中央 (132,120,110)、白邊 (242,238,235)，跟畫面看到的一致。
</details>

---

## 55 · 紙上時光調整：淡描圖紙、等寬白邊（2026-09-20）

- 描圖紙加一層 `overlayOpacity:.2`（原本的半透明度再乘 0.2），照片清楚很多，紙感變成很淡的提示
  而不是蓋住照片。這個欄位是新加的通用機制，其他載體沒設就是 1（不受影響）
- `photoInset` 從 `[6,6,6,22]`（拍立得式下寬）改成 `[5,5,5,5]`（沖印相片式等寬）；
  等寬的時候卡片比例＝照片比例，`ar` 跟著從 .82 改成這個 app 本來就用的 4:5（0.8）

實測：`.masklayer` 的 `opacity` = 0.2；卡片跟照片窗的寬高比一致（都是正方形，因為等寬邊框不改變比例）。

---

## 56 · 紙上時光再調整：邊框變窄變色、背景變亮、陰影收緊（2026-09-20）

- `photoInset` 從 5% 收到 **3.5%**（邊框變細）
- 邊框色從純白 `#fff` 改成 **`#f4f1eb`**（244,241,235）——
  比網站底色 `--paper`（`#ebe7dd`＝235,231,221）三個色版都亮一點點，
  是米灰不是純白，但也不會亮到跟頁面背景混在一起
- 新增 `dim:.2`（原本沒設，預設值 `.45`）：背景照片的黑色壓暗從 45% 降到 20%，
  背景亮很多，樹跟草地的細節看得出來
- `.card-bg` 的陰影模糊範圍收緊：`3px/22px` 模糊半徑改成 `2px/9px`，
  陰影更貼合卡片邊緣，不會暈開一大片

實測：邊框色 `rgb(244,241,235)`、底圖壓暗變數 `0.2`、陰影 `1px 2px` + `4px 9px`。

---

## 57 · 新濾鏡「舊派對」——修正暗部色偏方向（2026-09-20）

Lily 給了第二張參考截圖（老派對餐桌），指出「暗部在白色上會泛紫」。量出來之後，
**推翻了我在第一張截圖分析出的結論**：那時我說「暗部反而是最暖的地方」，
但那個結論是被畫面裡本來就偏黃綠的樹叢／草地污染的，不是真正的色彩分級。
這次用白色桌布（乾淨的中性參考面）重新量，結果完全相反：

| | 亮部（L>200） | 暗部落在中性面上 | 暗部落在暖色物體上 |
|---|---|---|---|
| RGB | 231/233/217 | 152/153/167、147/150/159 | 115/83/26 |
| R−B | +14（幾乎中性） | **−15（偏紫藍）** | +89（濁橄欖棕） |

**結論**：這不是兩種色偏，是同一個機制打在不同底色上——**暗部統一往深紫藍拉**，
落在白色這種中性面上看得出明顯的紫，落在本來就偏黃的物體（餐巾、銅燭台）上，
紫藍把原本乾淨的黃「弄髒」變成橄欖色。亮部维持接近中性、收得比金色那天乾淨很多
（金色那天亮部 R−B +79，這支只有 +14）。

**做法**：`.warm`（multiply，暖棕色）負責整體壓濁；`.violet`（**lighten**，深紫藍色）
負責「只把比這個顏色暗的地方墊上去，比它亮的地方完全不受影響」——
跟「陽光褪色」的暗部抬升是同一招，只是這裡選了藍紫色，剛好命中「暗部落在白色上會偏紫」。

<details>
<summary>調參數時踩到的坑：對比開太高會把紫色壓沒</summary>

第一版對比設 1.18、warm 不透明度 .5，測出來暗部直接被壓成死黑 (0,0,0)、
亮部卻被 warm 層拉暖到 R−B=+43（比目標 +14 高很多）。

**先誤判成程式壞了**——後來用「無」濾鏡（完全不調色）測同一張照片，
發現原始素材本身就有 10.7% 的像素是全黑（那隻狗旁邊的樹幹陰影，
手機拍照壓縮本來就會把很深的樹蔭壓死），跟我的新濾鏡無關，
是這張測試照片本身的特性——全黑的地方本來就沒有資訊可以救。

排除掉那些先天全黑的像素、改用非全黑的暗部樣本點直接比對調色前後：
確認 `.violet` 真的有作用（例如 30/33/27 → 42/39/48，R−B 從 +3 變 −6），
於是把對比降到 1.0、warm 不透明度收到 .16（multiply 對亮部的影響本來就比暗部大，
開太高亮部會被拉暖，跟「亮部要乾淨」的量測結果衝突）、violet 拉到 .6。

最終實測（同一張測試照片，非全黑樣本點）：
- 亮部 R−B：17～21（目標 14，很接近）
- 暗部 R−B：−7～−9（目標 −15，方向對、程度合理）
</details>

暫定名字「舊派對」，配方向她確認過，命名等 Lily 定案再改。

---

## 58 · 修正：紙上時光的照片窗內側有陰影（2026-09-20）

Lily 回報「照片匡內側不要有陰影」，附了一張截圖——白邊裡面、照片邊界處有一圈明顯的暗band。

**排查過程（一開始判斷錯了，中途修正）：**

1. 先猜是 `.card-bg` 的投影（box-shadow）暈過窄窄的白邊、滲到照片裡——
   拿掉投影之後陰影還在，判斷錯誤
2. 用一張純灰色、完全沒有內容的測試圖重新測：**完全乾淨，沒有任何陰影**。
   證實問題出在「照片內容本身」，不是紙上時光的程式構造
3. 問 Lily 截圖當時選的濾鏡——答案是「**舊派對**」（剛做好的新濾鏡，預設暗角 .4，
   是這批濾鏡裡數一數二重的）。真相大白：**那是舊派對自己的暗角**，
   套在紙上時光縮小很多的照片窗上，橢圓形暗角被放得很明顯，看起來像紙上時光加的陰影，
   其實任何暗角重的濾鏡（金色那天、陽光褪色…）搭紙上時光都會有同樣狀況

**修法**：紙上時光的照片窗（不是底圖那格）強制把 `--vig` 蓋成 `0`，不管選哪個濾鏡都一樣——
底圖那格維持濾鏡原本的暗角，只有卡片裡的照片窗乾淨。
`--vig` 是 CSS 繼承下來的值，寫在這一格自己身上就能局部覆蓋，不用動濾鏡本身的程式碼。

**順手清掉的東西**：`.card-bg` 的投影本來就沒用（不是真正原因，但也沒有存在的必要），
一併移除，改成純色平面。也記錄一個坑：曾經想用 `outline` 代替投影畫一圈細邊，
但輸出用的 canvas 渲染器不認得 `outline` 這個屬性（只讀 backgroundColor/backgroundImage/
boxShadow 這幾樣），會變成「預覽看得到、輸出影片不見」，所以放棄，改成純色平面。

**實測**：純灰色測試圖 + 舊派對 + 紙上時光，照片窗完全均勻、無陰影；
背景（框外）依然正常顯示舊派對的顆粒與色調，只有照片窗被單獨清掉暗角。

---

## 59 · 選中的濾鏡卡片改成紅色粗邊框（2026-09-20）

原本選中狀態是墨黑色 1px 邊框，太不顯眼。改成  紅色、邊框從 1px 加粗到 2px，
外圈再加一圈同色的 box-shadow 加強輪廓。

---

## 60 · 新載體「片格之間」——直式專用、模擬底片露出鄰格（2026-09-20）

同一張素材重複三次：中間正常置中顯示，上緣窄縫「置底對齊」、下緣窄縫「置頂對齊」，
模擬真的底片上下露出鄰格畫面的樣子，兩條裝飾線橫向拉滿蓋在交界處。

**量測**（Lily 的參考截圖，774×1366）：兩條線位置 y 13.8%~15.4%、83.7%~85.1%；
線條素材（原圖 2172×286）裁到線本身所在的 y 85~165 範圍，顏色深咖啡帶紅黑 RGB(23,3,0)。

**跟前面幾個載體都不一樣的地方**：不是「不同格放不同素材」，是同一個素材重複三次，
所以 `firstIndex()` 不當它是「底圖+窗」（不跳過第一張）；也沒有一張形狀圖，
兩條線是獨立的裝飾元素，`applyMask()` 完全不處理這個載體，line 由 `buildWrapZones()` 自己建。

**置底／置頂怎麼做的**：輸出用的 canvas 渲染器不支援 `object-position`
（`paintMedia` 算 cover 的時候一律置中），完全不能靠它做「只看得到底部/頂部」的效果。
改用「撐大＋位移」：把 `.media` 撐成跟整個畫面一樣高（相對這一窄格自己的高度換算成很大的
百分比），covering 用的縮放因此跟中間主體同一個鏡頭感；置底對齊再用 `translateY` 把撐大的
`.media` 往上推，讓它自己的底邊對齊這一窄格的底邊；置頂對齊不用位移，`top:0` 本來就是
「頂邊對頂邊」。這招全部是 transform／尺寸，兩個渲染器都讀得到，預覽跟輸出才會一樣。

<details>
<summary>踩到的兩個坑</summary>

**1. 位移公式多除了一次高度。** 第一版寫 `shift=-(100-h)/h*100`，結果整個窄格被推空、變成純黑。
`translateY(%)` 是照元素「自己」的高度算，元素自己的高度已經被撐成跟畫面一樣高（絕對值 100），
所以直接寫 `-(100-h)` 就對了，不用再除一次 h。

**2. 驗證輸出時一度以為程式全黑壞掉。** 用 cmux 內建瀏覽器測 `drawPreview()`，
量出來整個 canvas 是純黑（0 個非黑像素）——查了老半天，發現是**那個瀏覽器視窗本身縮成了
0×0**（`innerWidth`/`innerHeight` 都是 0），跟程式完全無關。換成不依賴視窗大小的
same-origin headless 測試，正確渲染：上緣 RGB(40,62,39) 深綠（草地）、
下緣 RGB(249,245,242) 亮白（天空/建築），跟畫面上看到的一致。
</details>

**新增「橫式尺寸會 disable」的機制**：`FRAMES[id].portraitOnly` 這個旗標，
選到這個載體就把「比例」下拉裡 `ar>=1` 的選項（1:1、16:9）標成 `disabled`，
如果當下剛好停在橫式會自動切回 4:5。門檻跟「今天的小星星」判斷要不要切左右對切用同一個。

**順手補的東西**：自製下拉選單原本完全不讀 `<option disabled>`（沒有任何選項曾經被鎖過個別項目，
只鎖過整個下拉），這次補上——`render()` 會把 disabled 的選項變灰、擋掉點擊，鍵盤 Enter 也擋。

實測：`#aspect` 選單四個選項 `[0.8:false, 0.5625:false, 1:true, 1.7778:true, auto:false]`，
橫式兩個正確被鎖。

---

## 61 · 修正：片格之間的線條被壓扁（2026-09-20）

Lily 回報：「線的圖檔是要直接壓在接縫上，不是把它壓縮成長方形」。

**原因**：第一版把線的高度寫死成量測值（1.6%/1.4%，量自參考截圖），
再用 `object-fit:fill` 硬塞進那個高度的框——線條素材自己的長寬比（2172:80＝27.15）
跟那個框完全對不上，整條被垂直壓扁，原本粗細不均、帶結痕的紋理變成一條死板的矩形。

**修法**：線的高度改成「橫向拉滿 100% 寬之後，照線條素材自己的長寬比反推該有多高」，
不是寫死的數字：
```
lineH = (100 / lineAR) * currentAR()
```
`lineAR` 是線條素材自己的寬/高（2172/80），乘上目前畫面的比例（`currentAR()`）
是因為「佔畫面高度的 %」本來就要看畫面本身多寬多高，畫面比例一換，這個數字要跟著變，
不然直式跟窄的 9:16 會算出不一樣粗細的線。

兩條線的**中心點**位置維持量測值（14.6%、84.4%，取自參考截圖兩條線的中點），
粗細不再量測，改成即時算出來的。

**實測**：線的核心厚度 18px（原本量到的量測值換算約 14.7px，在合理誤差內）；
放大看得到線條原本的紋理細節（紅棕色結痕、粗細不均），不是壓扁的矩形。

---

## 62 · 片格之間：素材數量提示 + 修正下拉選單捲動就自己關掉（2026-09-20）

**素材數量提示**（Lily 的規格，不是 bug）：片格之間三格用的是同一張素材，
其他張加了也不會被用到。新增 `FRAMES.wrap.singleItem:true`：
- 已經有一張素材時，「＋」變灰、點下去彈出說明（不會真的打開檔案選擇視窗）
- 已經加了多張素材才切過來的話，除了目前使用中那張，其他縮圖壓淡到 42% 透明度
- 要換照片：點掉現有那張的「×」，「＋」就會恢復正常

**修正：下拉選單長到要內部捲動時，一捲就自己關掉。**
Lily 回報「清單很長，滑不到片格之間，會被切掉」——原因不是清單真的少了選項
（開清單直接數，13 個都在），是有一個「偵測到任何捲動就關掉清單」的機制，
用 capture 監聽全域 `scroll`，結果連**清單自己內部的捲動**都被當成觸發條件。
清單選項多到超過 `max-height`（13 個 × 30px 已經超過 340px 上限）需要內部捲動時，
使用者一捲，捲動事件被同一個監聽器抓到、清單立刻自己關掉——感覺就像「片格之間」
這種排在清單後段的選項被切掉、捲不到。

修法：判斷捲動事件是不是從清單自己裡面發出來的，是的話不要關。

實測：模擬把清單捲到底（`scrollTop=58`），清單維持開啟；縮圖 class 正確標示
`on`（使用中）／`dim`（未使用、壓淡）；「＋」按鈕正確變成 `disabled`，
點擊跳出說明訊息、不會打開檔案選擇視窗。

---

## 63 · 拿掉片格之間的素材數量限制（2026-09-20）

Lily：「沒必要」。拿掉 `FRAMES.wrap.singleItem:true`，「＋」跟縮圖都恢復正常，
可以照常加任意數量的素材（片格之間本身還是只會用「目前這張」，只是不再特別提示/擋掉了）。

`renderItems()`／`markCurrent()` 裡讀 `singleItem` 的判斷邏輯留著沒刪——
現在沒有任何載體設這個旗標，等於整組是備而不用的死碼，之後真的要用再加回一行就好。

---

## 64 · 修正：片格之間的線變成一個「框」（2026-09-20）

Lily：「png 不要做任何修飾，直接壓在素材跟素材中間的縫隙上，不要有個框」。

**原因**：上一版把線當成「自己佔一格」處理——兩段照片各自停在線的上緣/下緣（`t0`/`t1`/`b0`/`b1`
四個不同的邊界），中間空出線本身的高度給它。這個做法的問題是：線的框裡只要有一點透明，
底下什麼都沒有（`.frame` 預設背景是黑的），看起來就像線外面多包了一層黑色的框。

**修法**：兩段照片直接貼齊（用同一個接縫位置 `m1`/`m2` 當邊界，中間完全不留縫），
線改成**疊在接縫正中央的裝飾**，不佔自己的格子，DOM 順序排在兩段照片後面（＝蓋在最上層）。
線本身透明的地方，直接透出接縫兩邊的照片，不會有黑色的框。

**實測**：接縫附近逐像素掃描，線的核心只有 6px 厚，緊貼在兩段照片內容之間，
前後都直接是照片內容，沒有額外的黑色縫隙。

---

## 65 · 舊派對：亮部加發光效果（2026-09-20）

Lily：「舊派對 針對最亮的部分，要有發光效果」。

**做法**：沿用金色那天／霧裡微亮同一招（`bloomLayer`）——複製一份照片，疊一層模糊＋加亮、
用 screen 混合模式蓋回去，只有亮的地方會被「點亮擴散」，暗部幾乎不受影響。
跟金色那天不同的是**不額外加色**：金色那天故意疊 sepia＋saturate 讓光暈變金黃，
但舊派對的亮部量測起來要偏中性乾淨，所以這裡只用「模糊＋提亮」，不改色調。
發光層疊在陰影紫調／濁暖層「之前」（DOM 順序），讓溢出來的光也會被後面兩層一起染色，
不會變成一團跟畫面調性脫節的乾淨白光。

新增兩個參數：「發光強度」（預設 .4）、「發光範圍」（預設 .5）。

**實測**：合成一張中心亮、四周暗的測試圖，比對開/關發光的兩張截圖——
亮點半徑 20～80px 範圍內，開啟後明顯更亮且更柔（例如某測點 100→173），
離亮點越遠差異越小，暗部幾乎沒變化。符合「只對最亮的地方發光」。

---

## 66 · 舊派對：顆粒調淡（2026-09-20）

Lily：「就派對顆粒0.06」。顆粒預設值從 .22 改成 .06，其他不動。

---

## 67 · 片格之間：支援 16:9 橫式（整個格局轉 90 度）（2026-09-20）

Lily：「16:9 也可以用這特效欸，只是就 90 度左邊的那格至右，右邊那格至左；1:1 效果不好維持 disable」。

**原本的限制**：`portraitOnly` 旗標會把「比例」下拉裡所有 `ar>=1` 的選項都灰掉（含 1:1 跟 16:9），
只放行直式比例。

**改法**：旗標改名 `noSquareOnly`，只擋 1:1 正方形，16:9 放行。`buildWrapZones()` 新增
`landscape=currentAR()>=1` 判斷：
- 直式：三格是「上/中/下」，上緣置底對齊、下緣置頂對齊（原本邏輯不動）。
- 橫式：三格改成「左/中/右」，左緣置右對齊、右緣置左對齊——跟直式是同一套
  撐大＋位移算法，只是把「撐高、translateY」換成「撐寬、translateX」，公式數字完全對稱。
- 兩條裝飾線本來是橫向拉滿、疊在上下接縫；橫式時線要改成直的、疊在左右接縫。
  沒有另外準備一張直式素材，直接用同一張線圖套 `transform:rotate(90deg)`——
  轉之前先照「轉正後應該長怎樣」反推轉正前的寬高（寬＝100/currentAR%、高＝100/lineAR%，
  中心點對齊接縫），再整個轉 90 度。canvas 匯出的 `applyTx()` 讀的是
  `getComputedStyle` 解出來的完整 transform 矩陣（不是分別解析 translate/rotate），
  所以直接套 CSS rotate，預覽跟匯出兩邊會轉成一樣的結果，不用另外處理。

**實測**：合成一張左紅右藍的漸層測試圖，切到片格之間＋16:9——
比例下拉正確顯示「1:1 灰掉、16:9 可選」；三格 DOM 量出來的寬度／位移數字跟公式反推的完全一致；
線條元素的 `naturalWidth/Height` 確認資源有載入（2172×80），實際渲染出來的
`getBoundingClientRect` 是一條貼齊接縫、跨滿整個畫面高度的窄直條，放大截圖確認
線條本身的紋理（像底片齒孔的一格一格）是直的、沒有被壓扁。

---

## 68 · 修正：片格之間中間格沒有置中（2026-09-20）

Lily：「16:9 中間那張有左右撐滿致中對齊嗎」——問了才發現真的沒有。

**原因**：`buildWrapZones()` 的中間格跟兩邊窄縫共用同一段程式碼，只是傳
`alignEnd=false` 表示「不用額外位移」。但這個「不用位移」的預設值（left/top:0）
其實是「貼齊起始邊」，只對下緣／右緣那個窄縫是對的（它本來就該貼齊接縫的起始邊）；
中間格套用同一個預設值，等於也被推去貼齊起始邊，多出來的撐大範圍整段被裁在另一側，
畫面因此偏向起始邊（橫式時偏左、直式時偏上），不是真正的「以照片正中央為準置中」。

**修法**：把 `alignEnd` 布林值改成 `align` 三選一（`'head'`／`'mid'`／`'tail'`），
中間格改用真正的置中公式：`shift = size/2-50`（size 是這一格佔畫面的百分比）。

**實測**：合成一張帶正中央十字記號＋左右邊界撞色的測試圖，量測中間格的
`zoneRect`／`mediaRect`——修正前兩者中心點差了 36px（偏向起始邊），
修正後兩個中心點完全對齊；放大截圖也確認正中央的記號剛好落在畫面正中央。
直式模式共用同一段程式碼，一併驗證中間格中心點也對齊了。

---

## 69 · 修正：輸出時切走分頁，存出來的影片會「掉幀」（2026-09-20）

Lily：「我存下來的圖會掉幀，幫我檢查一下原因」。

**原因**：顆粒亂位、手震、閃爍這幾個逐格效果（`grainTick`／`weave`／`flicker`），
底層都是走 `timers().raf()`，用的是 `requestAnimationFrame`。瀏覽器分頁切到背景
（例如按了「下載影片」之後切去做別的事）時，`requestAnimationFrame` 會被整個凍結、
完全不會再觸發——但影片輸出自己的擷取迴圈是用 `setTimeout`（`exportVideo()` 裡的
`tick()`），不受影響、還是繼續在錄。結果就是：分頁被切走的那段時間，錄下來的每一格
畫面幾乎一模一樣（顆粒沒在動、手震沒在抖、亮度沒在閃），播放起來像是那一段被卡住／
跳格，這就是「掉幀」的來源。下載圖片（`exportStills`）雖然自己的擷取迴圈本來就
刻意避開了 `requestAnimationFrame`（見程式碼裡原本的說明），但效果本身一樣會被凍結，
只是靜態圖比較難察覺。

**修法**：`timers().raf()` 平常（預覽階段）還是用 `requestAnimationFrame`——這是對的，
分頁切走時本來就該省電暫停。只有 `proj.exporting` 開著（正在輸出）的時候，改成用
`setTimeout` 頂替，讓這些逐格效果在分頁被切走時也不會被凍結。跟 `exportStills()`
自己擷取迴圈的做法是同一招，只是這次補到「效果」這一層，不是「擷取」那一層。

**實測**：攔截 `requestAnimationFrame`／`setTimeout` 計數確認分支切得對——
預覽模式（`exporting=false`）只呼叫 `requestAnimationFrame`，`setTimeout` 完全沒用到；
一把 `proj.exporting` 打開，同一個 `tick()` 立刻改成只呼叫 `setTimeout`、不再呼叫
`requestAnimationFrame`。分頁凍結 `requestAnimationFrame`是瀏覽器本身的行為，
沒辦法在測試裡直接重現「切分頁」，但只要輸出時完全不會再依賴這條會被凍結的路徑，
問題就不會發生。

**給 Lily 的建議**：這個修正讓「掉幀」不會再發生，但輸出影片／圖片時**還是不要切去別的分頁**
會比較保險——瀏覽器分頁背景化之後，就連 `setTimeout` 本身也可能被稍微放慢
（不會整個停掉，但可能沒那麼準時），輸出時留在畫面上等它跑完最穩。

---

## 70 · 修正：輸出影片還是卡（真正原因：可變幀率，QuickTime 撥放器踩雷）（2026-09-20）

Lily 回報上一版修完後「重新出圖還是卡卡的」。追問後確認：**全程都留在畫面上**（不是切分頁）、
**整支從頭卡到尾**（不是特定幾段）、用 **QuickTime／預覽直接雙擊打開看**。
這三個線索合起來，跟上一版（entry 69）修的「切分頁凍結」是不同問題。

**原因**：`exportVideo()` 原本用 `c.captureStream(0)`（0 表示「不自動送格，我自己手動控制」）
配合 `track.requestFrame()`，想法是「固定間隔手動推一格」比較穩。但這樣寫出來的每一格
時間戳，記的是「JS 實際呼叫 requestFrame() 的那一刻」——只要中間有一點點誤差
（系統排程、那一格剛好畫得比較久），幀距就會忽長忽短，變成「可變幀率」（VFR）的影片檔。
**QuickTime／預覽這類播放器對 VFR 檔案特別不友善，容易整支撥放起來一頓一頓**，
即使檔案內容本身沒有真的漏掉任何一格畫面也一樣——這是很多螢幕錄影工具、
瀏覽器產生的影片檔案都會踩到的已知相容性問題，跟 entry 69 修的「切分頁」是兩件事，
所以上一版修完她還是覺得卡。

**修法**：改成 `c.captureStream(FPS)`（給非 0 的固定幀率），讓瀏覽器自己用穩定的頻率
去「採樣」畫布當下的樣子，不用我們手動呼叫 `requestFrame()`。這樣輸出的每一格
時間距離永遠一樣（真正的固定幀率／CFR），最差狀況只是某一格重複用到上一格的畫面
（頂多看起來像瞬間定格一下），不會是整支忽快忽慢的頓挫感。

**實測到哪裡**：改完程式碼語法檢查過關（`node --check`）；也另外攔截
`requestAnimationFrame`／`setTimeout` 確認之前那個「切分頁」的修正邏輯還是正常運作，
兩個問題互不影響。**但這次沒辦法在我的測試環境裡把一支真的影片完整錄完再重播出來驗證**——
我用的無頭瀏覽器測試工具本身不支援錄影這個功能（缺 GPU，卡在畫面擷取那一步），
沒辦法在關掉前跑出真正的影片檔案。**這部分請 Lily 實際重新輸出一次、
一樣用 QuickTime 打開看看是不是順了**，這樣我才能確認真的修好，不只是邏輯上合理。

**後續**：Lily 傳了兩支重新輸出的檔案，量出來的數字跟改之前幾乎一樣（4fps、幀距忽快忽慢）——
一查才發現是我自己的疏失：程式碼確實改好了，但**忘記推上 GitHub**，Lily 測的網站版本
其實還是完全沒改過的舊程式碼。第二次確認並推上去之後才是真的部署了這個修正。

---

## 71 · 修正：輸出影片有時候整段都是橘色光（漏光入場還沒退完就開始錄）（2026-09-20）

Lily：儲存已經不會掉幀了（大突破），但「有時候整張都是橘色光」，不太穩定。

**原因**：`exportVideo()` 開始輸出前，如果畫板剛好停在上一輪播放完的「結尾」畫面
（漏光停格，一整片深橘褐 `rgb(132,38,2)`），會呼叫 `resumeFromEnd()` 重新歸位、
並重播一次「漏光入場」的動畫——這段動畫有 30 格、要跑將近 1 秒才會從整片橘紅色
退乾淨。但原本的程式碼在這裡只固定等 150ms 就繼續往下錄，等於常常在入場動畫
還沒退完、整格都還是橘色的時候就開始擷取畫面，看起來像整支影片都是橘色光——
而且**只有「畫板剛好停在結尾」才會踩到**，所以時好時壞，跟 Lily 形容的「不穩定」吻合。

**修法**：讓「漏光入場」的播放函式回傳一個 Promise，真正播完（display 收起來）才 resolve；
`exportVideo()` 改成**真的等這個動畫播完**才繼續，不再用猜的固定等 150ms。
（下載圖片 `exportStills()` 不用改——它是直接把入場那層強制關掉、不等動畫，
因為靜態圖本來就不需要「入場過程」這種時間性的效果，原本就處理對了。）

**同時做的：輸出面板改成置中對話框＋進度條**（Lily 原話：「儲存可能需要按下鐵盒，
展開 dialog 在螢幕正中間，背景操作面板蓋上黑色 40% 遮罩，並且在確定按下儲存按鈕時，
開始跑進度條，以及文字顯示，轉存中請不要離開此頁面」）：
- 輸出面板（`.expanel`）從原本釘在右下角，改成用 `left/top:50%` + `translate(-50%,-50%)`
  真正置中在螢幕正中間。
- 新增一片 `.expbackdrop`（40% 黑遮罩），跟面板共用同一個開關（`open()` 裡一起切換
  `data-open`），蓋住鐵盒跟底下的操作面板。點遮罩＝點「面板外面」，沿用原本就有的
  「點外面關閉」邏輯，不用另外寫。
- 面板裡新增一條進度條（`.expprogress`），輸出開始（`setBusyUI()`）才打開、結束就收起歸零；
  下載影片／下載圖片兩條路徑都會即時更新它的寬度（影片按已經過秒數算，圖片按張數算）。
- 狀態文字統一改成「轉存中，請不要離開此頁面 · ……」開頭，把提醒放在最前面。

**實測**：用同一套無頭瀏覽器測試——面板置中的座標量出來剛好等於畫面正中央
（把 CSS 動畫關掉量測，避開動畫過程中量到中間值的干擾）；遮罩的 `data-open` 開關、
透明度都如預期；實際跑一次「下載圖片」流程，狀態文字跟進度條數字都正確
（例如「轉存中，請不要離開此頁面 · 圖片 2 / 3」對應 33.3% 的進度條寬度）。
**橘色光那個修正沒辦法像上面一樣錄一支真的影片來驗證**（同樣卡在無頭瀏覽器不支援
錄影），只做到程式碼邏輯推導＋語法檢查，麻煩 Lily 實際測一次確認。

---

## 72 · 輸出對話框放大、改左右兩欄（2026-09-21）

Lily 看到新的置中對話框後：「框框可以大一點，可能是左右排版，影片放左邊可以大一點，
右邊目前資訊比較少，之後會補上、補齊」。

**做法**：
- `.expanel` 從 236px 寬（單欄堆疊）放寬到 `min(600px,94vw)`。
- 內容改成左右兩欄（新增 `.expbody`／`.expinfo` 兩層包裝）：左邊預覽圖固定
  300px 寬，右邊資訊欄（規格、按鈕、進度條、狀態文字）吃剩下的寬度。
- 預覽圖本身也放大——`drawPreview()` 畫布解析度上限從 204px 提到 300px，
  不只是 CSS 顯示尺寸變大，實際畫進去的解析度也跟著提高，放大看才不會糊。
- 窄螢幕（手機，≤640px）用 `@media` 疊回原本上下堆疊的排法，不會被硬擠成兩欄。

**過程中順便抓到一個舊坑**：手機版（≤860px）「三段式」版型那段裡，還留著一條
更早以前、給「釘在右下角小面板」時代用的規則 `.expanel{left:12px;right:12px;
bottom:72px;width:auto}`，蓋掉了新版置中對話框的定位，手機上會整個歪到左上角、
寬度也不對。已經連這個一起拿掉。

**實測**：改完直接量測（用同一套無頭瀏覽器＋關掉 CSS transition 避開動畫過程中量到
中間值的干擾）——桌機寬度 1000px 下量到面板真的變寬、預覽圖跟資訊欄左右並排；
手機寬度 390px 下量到面板寬度正確落在 340px、真的置中在畫面正中央（拿掉舊坑之前
量到的是寬度 366px、貼齊左邊 12px，改完後確認變成置中），排版也正確疊回上下堆疊。

---

## 73 · 輸出還是卡：抓到「量測幀率」這一步本身在騙自己（2026-09-22）

Lily：「轉存還是卡卡的」「之前存都正常啊，是不是因為濾鏡太多？」。

**先講會不會是「濾鏡太多」**：方向是對的——但不是「下拉選單裡濾鏡種類太多」，
是「單一濾鏡本身疊了好幾層有模糊效果的畫面」（發光、柔焦），每一層都要花真的時間畫，
疊越多層一格要花的時間越長。

**抓到的問題**：`exportVideo()` 開始錄之前，會先畫幾格「暖機」，量一下畫一格平均要
多久，藉此決定要用幾 fps 輸出。但這個量測方法有個陷阱——瀏覽器很多時候會把畫布的
繪圖指令「延後處理」，只有真的要讀取畫面內容（例如被錄影擷取那一刻）才會動手畫。
暖機的時候只有「發出畫圖指令」，沒有真的去讀畫面，量到的時間因此不準、偏快太多。

**實測抓包**：拿一個 1080×1920 的畫布單獨測「發光模糊」那一層的疊圖效果——
沒強制讀取畫面：量到 0.00ms（假象，指令根本還沒真的執行）；
強制讀取畫面一次：量到 42–48ms（這才是真的要花的時間）。
差了幾十倍。原本 `exportVideo()` 的暖機也踩在同一個陷阱裡，所以常常選出偏高的幀率
（例如 30fps），但真正錄的時候跟不上，才會忽快忽慢／卡頓——跟上一版修的「可變幀率」
是同一個症狀的另一個成因，這次才抓到根。

**修法**：暖機那幾格，每畫完一格都强制讀一次畫面（`ctx.getImageData`），逼瀏覽器
真的把這一格畫完，量到的時間才是真的，選出來的 fps 才會誠實。狀態文字也把 fps
數字加回來（`轉存中… 還有 X 秒 · Y fps`），方便下次匯出直接看到數字。

**沒辦法在我這邊完全驗證的部分**：用同一個「舊派對」「金色那天」在我的測試環境裡
量暖機，量到的還是偏快（30fps）——但我這邊沒有真的影片編碼的環境（一直卡在同一個
「無頭瀏覽器不支援錄影」的牆），沒辦法重現「畫面＋真的錄影同時發生」那個更重的組合
負擔，也沒辦法用 Lily 實際的照片素材測（我的測試圖是簡單的漸層色塊，可能比真實照片
輕很多）。這次修正**保證量出來的 fps 數字是誠實的**，但沒辦法保證那個數字一定夠高。

---

## 74 · 試驗：時間光漏改用「一格一格算、不即時」的新輸出管線（2026-09-23）

Lily：影片還是卡、結尾轉場也怪，問要不要換方法。討論後決定先拿「時間光漏」一個
濾鏡當白老鼠試新做法，行得通再擴大到其他濾鏡。

**新做法是什麼**：原本「下載影片」是一邊播放一邊即時錄下來（`MediaRecorder`＋
`captureStream`），電腦要跟得上真實時間才錄得順。新做法完全不即時——自己控制一個
「虛擬時鐘」，一格一格往前推、每格愛畫多久畫多久，畫完才進到下一格，最後把整串畫面
接成影片。因為不用跟真實時間比賽，電腦再慢也只是「多花一點時間輸出」，不會反映成
影片本身卡頓。

**做了什麼**：
- 新增 `makeVirtualTimers()`：跟原本的 `timers()`（管顆粒、手震、漏光這些逐格效果）
  介面一模一樣，差別是完全不碰真的 `setTimeout`／`requestAnimationFrame`，時間都是
  自己說了算，用 `.step(目標時間)` 往前推進。
- 「時間光漏」的漏光閃爍效果，原本是設好 CSS transition 讓瀏覽器自己過渡（淡入淡出），
  改成每格自己算目前該多亮（ease-out／ease-in 用簡單的平方曲線近似）——因為新管線沒有
  真實時間可以讓瀏覽器自己跑過渡動畫，只能自己決定「這一格該長怎樣」。
- 新增 `exportVideoLeak()`：只有選到「時間光漏」時，「下載影片」按鈕會走這條新路，
  其他濾鏡繼續用原本的做法（都還沒動）。
- 影片編碼＋封裝成 mp4 用的是 `mediabunny`（透過瀏覽器內建的 WebCodecs，從 CDN 載入，
  不用安裝任何東西，跟這台電腦「零安裝出圖」的慣例一致，只是這次是直接在瀏覽器裡做，
  不用另外開小程式）。

**目前先簡化、還沒做的部分**（先求跑得通，之後再補）：
- 場景之間的換場效果（漏光燒片／交叉淡入）先跳過，直接硬切——換場動畫本身也是
  用真實時間／CSS transition 在跑，要讓它變成「準確算出第幾秒該長怎樣」是另一塊工程。
- 入場、結尾的漏光效果同理，先關掉。
- 目前固定輸出 24fps（不像原本的做法會先「量一下電腦多快」，反正新做法不怕電腦慢，
  直接给一個順眼的速度就好）。

**實測**：合成一張測試圖，實際跑一次「下載影片」——CDN 元件正常載入、編碼跟封裝
都成功跑完，存出一支 2 秒、1080×1350、2.5MB 的 mp4。用 Mac 內建工具（跟前幾次
量舊做法用的同一套）逐格量時間戳：**49 個間隔，48 個都精準落在 0.0417 秒
（=1/24 秒）不差分毫**，完全沒有忽快忽慢——這是目前為止量到最乾淨的結果，
跟之前那些「9fps 但偶爾跳到 0.47 秒」的舊檔案差很多。

**還沒驗證到的部分**：這次是用合成的測試圖跑的，還沒有用 Lily 實際的照片、實際的
素材數量測過；也還沒有測試「換場效果先跳過」這件事在她眼裡看起來順不順（可能會覺得
硬切怪怪的）。這些要等她實際用過才能確認。

---

## 75 · 試驗（續）：新輸出管線接上真的換場動畫（漏光燒片）（2026-09-23）

接著 entry 74 繼續做，這次把「跳過的換場效果」接回來，不再是硬切。

**做了什麼**：
- 換場（`nextScene()`）另外開一個獨立的虛擬時鐘 `exportTransVT`，跟畫面本身的
  `exportVT` 分開——原因是每次切到新照片都會 `build()`，`build()` 會把上一格的
  虛擬時鐘清空，換場動畫如果跟畫面共用同一個時鐘，播到一半會被這個清空動作打斷。
  分開之後，新輸出管線可以直接重複使用 `nextScene()` 原本就寫好的燒片腳本
  （黑場→燒片顏色→黑場→過曝回穩），不用整個重寫。
- 「交叉淡入」那個換場方式目前還沒完全接上（它的淡入淡出是靠 CSS transition 算，
  虛擬時鐘沒辦法讓它跟著走）——但「時間光漏」預設用的是「漏光燒片」，不受影響。

**實測**：兩張顏色不同的測試圖（紅色、綠色），跑一次完整輸出，這次不只量時間戳，
還逐格量了畫面的平均顏色，確認「燒片」真的有照劇本演出來：
第 1 張紅色場景 → 黑場 → 燒片橘黃色（燒了兩格不同的橘）→ 黑場（換場那一刻，
背後已經切到第 2 張） → 過曝閃一下 → 第 2 張綠色場景，順序完全正確。
時間戳一樣完美：76 個間隔，全部精準落在 1/24 秒，沒有一格例外。

---

## 濾鏡的參考影片對照

| 濾鏡 | 參考影片 | 備註 |
|---|---|---|
| 時間光漏 | ✅ | 同一支也生出「漏光入場」「漏光停格」與「褪色暖調」 |
| Super 8 | ✅ | |
| 8mm 放映機 | ✅ | |
| 陽光褪色 | ✅ | |
| 花田時光 | ✅ | |
| 方寸之間 | ✅ | |
| 黑白老電影 | ❌ | 沒有參考影片，照默片的物理原則做的 |
| 褪色暖調 | ↑ | 與時間光漏共用同一支 |

（Lily 手上曾有兩支檔名對調 —— 用亮度分位、飽和、平均 RGB 三項比對後確認並修正。）

---

## 方法上的原則（這次又驗證了幾次）

1. **能量的就不要用猜的。** 〈暗部抬升〉、窗緣界線、機身各面的亮度，都是量完才知道問題在哪。
2. **量測本身也會出錯。** 這次至少三次假警報：用 `mousedown` 測 `pointermove` 綁定的功能、忘記關掉上一張清單就量下一張、瀏覽器面板隱藏時 rAF 被節流。結論寫下來之前先確認量測方法本身是對的。
3. **看起來對不代表輸出對。** 輸出用的是另一套 canvas 渲染器，`mask-image` / `backdrop-filter` / `background-blend-mode` 它都不支援。新功能一律先確認輸出路徑畫得出來。

---

## 現在的資料夾結構

（取代 §10 的版本 —— 圖檔都已轉成 WebP，並新增了取景器）

```
Photo/
├── film_gallery.html      主程式（單一檔案）
├── serve.command          啟動本機伺服器（雙擊）
├── CHANGELOG.md
├── _整理紀錄_20260916.txt  檔案搬移／刪除的紀錄與還原方式
│
├── assets/                程式執行時載入（共 1.3 MB）
│   ├── logo@2x.webp           I SAW 布標
│   ├── lace@2x.webp           蕾絲
│   ├── iron.webp              右下角的輸出銀盒
│   ├── paper.webp             紙質紋理（無縫平鋪）
│   ├── finder.webp            方寸之間的取景器（Lily 手繪，有 alpha）
│   └── tracing-paper-1~3.webp Our Story 三格動畫
│
├── masks/                 底片框：12 個 SVG + slots.json
├── media/                 預設影片 default-1.mov / default-2.mov
├── tools/                 genmask.py（遮罩產生器）
│
├── source/                原始高解析檔，不參與執行
│   ├── tracing-paper-1~3.png  描圖紙原圖
│   ├── 方寸之間.mov            取景器的參考影片
│   ├── logo.png、蕾絲.png、iron.png、剪刀.png
│   ├── augustine-wong-...jpg  紙材原圖
│   └── 遮罩1/2.png、遮罩1/2.svg
│
├── my-media/              Lily 自己的照片與影片
└── experiments/           film_light_leak.html、super8_film.html
```
